# BentoSlide構成案：ランダムRTE回路のコンパイル後コスト推定検証

## 1. 制作依頼の要約

### 資料タイトル案

**ランダムRTE回路のコンパイル後コスト推定モデルと検証**

副題：

> DF＋部分ランダムコスト評価に向けた続編

### 前資料との関係

この資料は、`docs/research/DF+PRコスト評価.pdf`の続編だが、HTMLとしては別資料にする。
前資料では、PF誤差係数、有限RTEの解析誤差上界、RPE総コストへ至る変数関係を説明し、
「ランダム回路のコンパイル後期待コストをどう近似するか」を次の主要課題として残した。

本資料はその問いへの回答として、次の流れを説明する。

1. イベントコストの単純加算を検証し、大きな過大評価のため棄却した。
2. イベント境界の相殺を短い局所回路から推定する局所境界補正モデルを導入した。
3. 出現確率の低いTaylor次数、モンテカルロ分散、条件移送を切り分けて検証した。
4. H4の複数条件とH5の独立検証を通じて、条件ごとの3イベントモデルと必要時4イベント補正という運用規則を得た。

前資料の研究背景、PF係数$C$、有限RTEの演算子誤差・状態上の複素期待値誤差の検証は繰り返さない。冒頭1枚で
前資料との接続だけを示し、今回の回路コスト検証へ直ちに入る。

### 資料の目的

共同研究者に、次の4点を一続きで共有する。

- なぜランダム回路のコンパイル後期待コストに近似が必要か。
- どの単純モデルが失敗し、なぜ局所的な境界補正へ進んだか。
- どの条件でどこまで検証し、例外条件をどう扱ったか。
- 大きな系のコスト推定に、現時点でどのモデルをどう運用するか。

意思決定を求める資料ではなく、「ここまで検証し、今後はこの規則で総コスト評価へ接続する」
という研究進捗の共有資料にする。

### 中心メッセージ

> RTEイベントを個別にコンパイルして足す単純加算は使えない。一方、短い1--3イベントの局所回路から
> 境界相殺を較正する局所境界補正モデルは、H4・H5の指定条件で未使用の長い回路を
> おおむね5%以内に予測した。標準は3イベントまでとし、不通過条件だけ4イベント補正へ進む。

### 主張の範囲

- 数値はこのリポジトリ内で実行したローカル検証結果であり、外部環境や継続的統合環境で
  再現済みの結果ではない。
- H4・H5、H鎖1.0 Å、STO-3G、明示したDFランクと保存ハミルトニアンデータ、
  `rz,sx,x,cx`、最適化レベル1、量子ビット結合制約なしの範囲である。
- 量子測定回数、雑音、実機実行、状態準備、RPE全体および最終総コストは未評価。
- 約5%は運用上のモデル不確かさであって、数学的な厳密上界ではない。
- H12や別のコンパイル条件へH4/H5係数をそのまま移送できるとは主張しない。

### 用語上の重要な指示

- 本資料では、Qiskitで回路を変換した後のRZ/CXゲート数・深さ、全体深さ、回路サイズを
  「コンパイル後コスト」、以後は単に「回路コスト」と呼ぶ。
- `holdout`は使わず、「係数調整に使っていない未使用回路による独立検証」と書く。
- `paired`は使わず、「同一イベント列を使う対応あり構造検証」と書く。
- Taylor打切り次数は$K_{\mathrm{Tay}}$と表記する。
- 局所境界補正項は$\kappa_1,\kappa_2,\kappa_3,\kappa_4$と表記し、Taylor打切り次数と混同しない。
- `K1--K3 model`とだけ表示せず、初出では「1--3イベントの局所境界補正モデル」と併記する。
- `z値が大きい＝相対誤差が大きい`とは限らない。残差が統計的に非ゼロかと、実用上の大きさを分ける。
- 点ごとの正規近似95%上側値を厳密上界または全指標同時信頼上界と呼ばない。

### 推奨表示仕様
- 16:9、1280×720。
- 本編16枚。補足スライドは原則作らない。
- 数式、表、模式図、手順図は編集可能なHTML/CSS/SVGで作る。
- 数値グラフは使わず、比較は表と強調数字で示す。空いた領域は定義と解釈の説明に使う。
- 検証の経緯が重要なので、成功結果だけでなく「単純加算の失敗」「通常抽出による検証の不足」も本文に含める。


## 2. AIエージェントが最初に読む資料

資料生成前に、次を順に確認する。

1. `docs/research/研究概要・現状.md`
2. `docs/rte_compiled_cost_validation_summary.md`
3. `docs/random_circuit_cost_validation.md`
4. `docs/rte_boundary_cost_validation.md`
5. `docs/rte_boundary_pair_validation.md`
6. `docs/hierarchical_cost_validation.md`
7. `docs/rte_connected_cluster_cost_validation.md`
8. `docs/research/研究ノート/2026-08-25.md`
9. `docs/research/研究ノート/2026-08-26.md`
10. `VALIDATION_STATUS.md`と`artifacts/validation_manifest.json`

数値は`docs/rte_compiled_cost_validation_summary.md`から引用できる。表を作る際は対応する
機械可読データを直接確認する。実行再開用の一時保存やログは資料の根拠に使わない。

## 3. 資料全体のストーリー

```text
前資料で残ったコンパイル後期待コストの問題
        ↓
有限RTEが作るランダムイベント列と、列長Lを最小限説明
        ↓
イベント別コストの単純加算を検証
        ↓
48--58%過大評価：単純加算を棄却
        ↓
局所境界補正モデルを導入
        ↓
$K_{\mathrm{Tay}}=0$で2イベント・3イベント補正、同一・異種DFフラグメント境界を検証
        ↓
$K_{\mathrm{Tay}}=2$の低確率イベントを条件付き抽出で再検証
        ↓
同一イベント列を使う構造検証と、独立データを使う運用検証を分離
        ↓
H4の条件移送、例外L_D=6の4イベント補正、制御付き反復qを確認
        ↓
H5の独立した係数較正・未使用回路検証で系サイズ方向を確認
        ↓
総コスト最適化では、条件ごとの1--3イベントモデル＋不通過時4イベントという規則で回路コストを供給
```

## 4. セクション構成

| セクション | スライド | 役割 |
|---|---:|---|
| A. 前資料からの接続 | 1--2 | 今回の問いと範囲を定める |
| B. RTEと単純加算の検証 | 3--5 | RTEイベント列、基準、検証設計、棄却結果を示す |
| C. 局所境界補正モデル | 6--10 | 局所補正、Taylor次数、対応あり構造検証を説明する |
| D. 運用時の独立検証 | 11--15 | H4移送、4イベント補正、制御付き反復、H5を示す |
| E. 結論 | 16 | 採用規則と次工程を示す |

## 5. スライド別の詳細構成

### スライド1. タイトル

**タイトル**

> ランダムRTE回路のコンパイル後コスト推定モデルと検証

**副題**

> DF＋部分ランダムコスト評価に向けた続編

タイトルと副題だけを配置する。模式図、説明文、数値は入れない。

---

### スライド2. 前資料からの接続

**タイトル**

> 残っていた課題は、1回のランダム回路のコンパイル後期待コスト

**必須内容**

前資料の最終目的関数を再掲するが、説明は期待コスト部分に絞る。

$$
G_{\mathrm{total}}
=\sum_m\sum_{b\in\{c,s\}}N_{m,b}\,\mathbb E[C_{m,b}^{\mathrm{no\text{-}prep}}]
$$

- PF係数$C$：指定条件で検証済み。
- 有限RTE誤差上界：H4の指定条件群で検証済み。
- 今回：$\mathbb E[C_{m,b}]$を全イベント列のコンパイルなしで求める方法を検証。

**範囲外を下段に明記**

`測定回数との接続・β/α配分・最終総コストは今回の結果ではない`

**参照元**

- `docs/research/DF+PRコスト評価.pdf` 13--15、20--27枚目。
- `docs/research/研究概要・現状.md` 3節。

---

### スライド3. なぜ近似が必要か

**タイトル**

> 有限RTEはランダムイベント列を作るため、全列の一体コンパイルは難しい

**RTE回路の並びと、本資料で使う単位**

partial-$S_2$の1ステップでは、中央の$e^{-iH_R\delta}$を有限RTEで置き換え、回路は概念的に

$$
S_D^{\mathrm{rev}}(\delta/2)
\;\underbrace{V(e_L)\cdots V(e_2)V(e_1)}_{\text{RTEブロック}}\;
S_D(\delta/2)
$$

と並ぶ。

- イベント$e_i$：1回の短時間発展で抽選されるTaylor次数とハミルトニアン成分の組。
  $V(e_i)$は、その1イベントに対応する回路である。
- イベント列$\omega=(e_1,\ldots,e_L)$：一つのRTEブロックを作る一連の抽選結果。
- trajectory：コードや元データで使う名称。この資料では、1回の抽選で得たイベント列または
  その一体回路を指し、以後は「イベント列」と表記する。
- $L$：イベント列に含まれるイベント数。実際の1回のRTEブロックでは$L=r_m$である。
  今回の局所モデル検証では、係数用の$L=1,2,3$と未使用検証用の$L=4,6,8$を評価した。
- 本資料の「回路コスト」は、回路変換後のRZ/CXゲート数・深さ、全体深さ、回路サイズを指す。

**必須式**

$$
\mathbb E[C_L]
=\sum_\omega p(\omega)C_{\mathrm{comp}}(\omega)
$$

**説明**

- イベント種類数を$J$とすると列挙空間は概ね$J^L$。
- $C_{\mathrm{comp}}(e_1e_2)$は一般に$C_{\mathrm{comp}}(e_1)+C_{\mathrm{comp}}(e_2)$ではない。
- 基底変換、隣接回転、回路最適化がイベント境界をまたいで作用する。

**比較する3方式**

| 方式 | コンパイルする回路 | 位置付け |
|---|---|---|
| 一体回路のモンテカルロ | $V(e_L)\cdots V(e_1)$全体 | 検証基準だが重い |
| イベント単純加算 | 各$V(e_i)$を別々に処理 | 安いが境界効果を無視 |
| 局所境界補正 | 1--3イベントの短い部分回路 | 長い列の期待コストを再構成する候補 |

**このスライドで言うこと**

- 目的はモンテカルロそのものをなくすことではなく、コンパイル対象を短い局所回路に限定することである。

---

### スライド4. 検証設計と判定基準

**タイトル**

> 局所係数の較正と、係数調整に使わない未使用一体回路の検証を分離する

**基本フロー**

`短い局所回路で較正 → 係数固定 → 別の乱数シードで作った長い一体回路で検証`

この未使用回路による確認を、本資料では「独立検証」と呼ぶ。

**H4共通条件**

- H4鎖、1.0 Å、STO-3G、DFランク12。
- 保存した同一のDFハミルトニアンデータを使用。
- `rz,sx,x,cx`、最適化レベル1、乱数シード17、量子ビット結合制約なし。
- 主指標はRZ数。補助指標はRZ深さ、CX数・深さ、全体深さ、回路サイズ。

**暫定判定**
- 全指標最大点誤差5%以下。
- RZ予測側95%相対半幅2%以下。
- 点ごとの95%上側値は診断のみ。

5%は候補比較に使用できるかを判断する暫定的なモデル許容差で、理論から一意に決まる値ではない。
予測半幅2%は、係数推定の統計揺らぎを5%のモデル許容差より十分小さくするための暫定条件である。

**z値の読み方**

$$
z=\frac{|\text{予測値}-\text{一体回路の平均}|}
{\sqrt{\mathrm{SE}_{\mathrm{pred}}^2+\mathrm{SE}_{\mathrm{ref}}^2}}
$$

z値は差が標準誤差の何倍かを表す。目安として2を超えると、差を標本揺らぎだけでは説明しにくい。
ただし、実用上の誤差の大きさは相対誤差で判断し、z値だけで合否を決めない。
上式は独立検証の場合である。同一イベント列を使う対応あり検証では、列ごとに取った差の平均を
その差の標準誤差で割るため、同じ列に由来する共通の揺らぎを含めない。

**見せ方**

- 左に条件、中央に係数較正と独立検証の分離、右に判定指標。

---

### スライド5. イベント単純加算の検証結果

**タイトル**

> イベントを別々にコンパイルして足すと、48--58%過大評価した

**条件**

H4、DFランク12、$L_D=3$、$\delta=0.1$、$K_{\mathrm{Tay}}=0$、$r=2$、同一DF表現、100イベント列。

**結果**

比較した境界を二種類に分けて示す。

| 比較 | 分割の仕方 | 結果 | 意味 |
|---|---|---:|---|
| RTE内部の境界 | 各イベントを別々にコンパイルして足す 対 イベント列全体を一体コンパイル | 48.30--57.58%過大評価 | イベント間の相殺を失う影響が大きい |
| partial-$S_2$外側の境界 | 決定論前半・RTEイベント列全体・決定論後半を別々にコンパイルして足す 対 partial-$S_2$全体を一体コンパイル | 最大0.959%過大評価 | RTEブロックと決定論部分の境界効果は、この条件では小さい |

partial-$S_2$外側の比較を式で書くと、コンパイル後回路コスト$C$に対して

$$
C(S_D^{\mathrm{rev}})+C(\widetilde U_{\mathrm{RTE}})+C(S_D)
$$

と

$$
C(S_D^{\mathrm{rev}}\widetilde U_{\mathrm{RTE}}S_D)
$$

の差を測っている。この差が最大0.959%であり、前後二つの外側境界の効果をまとめた値である。

別の長さ検証では、イベント列長$L=6$でイベント単純和の最大誤差が157.51%まで増加した。

**推奨表**

RZ数、CX数、全体深さについて、「一体コンパイルを1としたイベント単純和の比」と相対誤差を示す。
表の下に、partial-$S_2$の3部分和は最大0.959%だったことを別枠で示す。

0.959%は「ランダム部分が小さいため、48--58%の誤差が全体で薄まった」という比較ではない。
RTEイベント列を一つのブロックとして残したうえで、その外側の二境界だけを切った比較である。

**結論ボックス**

> イベント単純和は不採用。大きな差は主にRTEイベント間の境界を切ったことで生じる。

**参照データ**

- `artifacts/random_circuit_cost_validation/h4_sto3g_d100_rank12_ld3_dt0p1_r2_k0_v1.json`
- `docs/random_circuit_cost_validation.md`

---

### スライド6. 局所境界補正モデル

**タイトル**

> 短い局所回路から、イベント境界の相殺を補正項として取り込む

**必須式**

$$
\widehat C_L
=\sum_i\kappa_1(e_i)
+\sum_i\kappa_2(e_i,e_{i+1})
+\sum_i\kappa_3(e_i,e_{i+1},e_{i+2})
+\cdots
$$

$$
\kappa_2(e_1,e_2)=C(e_1,e_2)-C(e_1)-C(e_2)
$$

$$
\kappa_3(e_1,e_2,e_3)
=C(e_1,e_2,e_3)-C(e_1,e_2)-C(e_2,e_3)+C(e_2)
$$

**直感**

- $\kappa_1$：単一イベントのコスト。
- $\kappa_2$：一境界で回収される相殺。
- $\kappa_3$：隣り合う二境界を独立に足せない3イベント残差。
- $\kappa_4$：3イベントまでで不足した条件だけ追加する4イベント残差。

**重要な表記**

スライドの隅に`Taylor打切り次数 = K_Tay / 境界補正項 = κ1,κ2,…`を固定表示する。
以後、$\kappa_2$までを「2イベント補正」、$\kappa_3$までを「3イベント補正」と呼ぶ。

---

### スライド7. $K_{\mathrm{Tay}}=0$：必要な境界補正の長さ

**タイトル**

> まずTaylor次数0だけに固定し、2・3イベント補正のどこまで必要か調べた

**調べたいこと**

低確率のTaylor次数2をまだ入れず、イベント境界を何イベントまで補正すれば、
未使用の長い回路を5%程度で予測できるかを調べる。

**条件**

H4、1.0 Å、STO-3G、DFランク12、$L_D=3$、短時間幅0.025、
$K_{\mathrm{Tay}}=0$、同一のDFハミルトニアンデータとコンパイル条件。

**比較方法**

| 予測 | 入れる補正 | 比較先 |
|---|---|---|
| 2イベント補正 | $\kappa_1,\kappa_2$ | 係数較正に使っていない$L=4,6,8$の一体回路 |
| 3イベント補正 | $\kappa_1,\kappa_2,\kappa_3$ | 同じ未使用一体回路 |

**結果**

- 独立2乱数シードの$L=4,6$：2イベント補正は最大8.07--8.18%、
  3イベント補正は最大2.33--3.73%。
- 未使用$L=8$・2000イベント列：2イベント補正のゲート数・回路サイズ誤差は最大8.851%、
  3イベント補正は全指標最大1.744%。
- 深さ指標では、2イベント補正の統計的な不適合は確認されなかった。

**結論**

> $K_{\mathrm{Tay}}=0$では、ゲート数・回路サイズは3イベント補正、
> 深さは2イベント補正も候補とする。

---

### スライド8. $K_{\mathrm{Tay}}=0$：2イベント補正の内部分類

**タイトル**

> 次に$\kappa_2$の中身を調べ、同一／異種DFフラグメント境界の両方を必要とした

**前スライドとの関係**

これは2イベント補正と3イベント補正をもう一度比較する検証ではない。
スライド7で採用候補に入れた$\kappa_2$の平均値を、境界の種類から正しく構成できるかを調べる。

**条件**

スライド7と同じH4、1.0 Å、STO-3G、DFランク12、$L_D=3$、短時間幅0.025、
$K_{\mathrm{Tay}}=0$。係数較正と未使用検証は各1500イベント対。

**比較方法**

- 同一DFフラグメント境界：隣接イベントが同じ基底変換を使う。
- 異種DFフラグメント境界：隣接イベントが異なる基底変換を使う。
- 「同一境界だけを補正」と「両境界の条件付き平均を解析確率で合成」を比較する。

$$
\overline{\kappa}_2
=p_{\mathrm{same}}\mathbb E[B\mid\mathrm{same}]
+p_{\mathrm{different}}\mathbb E[B\mid\mathrm{different}],
\qquad p_{\mathrm{same}}=0.7310604
$$

**結果**

| $\kappa_2$の作り方 | 最大誤差 | 最大z値 |
|---|---:|---:|
| 同一DFフラグメント境界だけ | 3.65% | 2.59 |
| 同一／異種境界を解析確率で合成 | 0.849% | 0.587 |

**結論**

> 実運用の$\kappa_2$には同一／異種の両境界を入れる。
> この分類は別モデルではなく、3イベント補正の中にも含まれる$\kappa_2$の較正方法である。

---

### スライド9. $K_{\mathrm{Tay}}=2$：低確率のTaylor次数2をどう抽出するか

**タイトル**

> $K_{\mathrm{Tay}}=0$の基礎検証から進み、次数2イベントを意図的に含める抽出方法を定めた

**調べたいこと**

$K_{\mathrm{Tay}}=2$で新たに現れるTaylor次数2イベントを、通常抽出の偶然に頼らず、
期待回路コストの検証に必ず含める方法を決める。

**条件**

H4、1.0 Å、STO-3G、DFランク12、$L_D=3$、短時間幅0.025、
$K_{\mathrm{Tay}}=2$、イベント列長$L=4,6$。

**通常抽出の問題**

$$
p_{2}=1.062925\times10^{-4}
$$
- 旧計算の8000イベント位置中、Taylor次数2は1回しか現れていなかった。
- したがって旧結果は、$K_{\mathrm{Tay}}=2$を指定していても実質的に次数0の列を検証していた。

**採用した抽出方法**

| 分類 | 抽出方法 | 初期再検証の標本数 |
|---|---|---:|
| 全イベントが次数0 | 条件を指定して直接生成 | 各$L$で500 |
| 次数2が1か所 | 次数2の位置も指定して直接生成 | 各位置で100 |

各分類の平均回路コストに、その分類が本来現れる解析確率を掛けて再結合する。

**次数2が2か所以上の扱い**

初期検証では全配置を生成したが、各配置2回は回路生成とコンパイルの動作確認であり、
この分類単独の統計精度を評価したものではない。
実際に使うイベント列長で解析確率が無視できない場合は、改めて標本数を配分する。

**このスライドの結論**

> $K_{\mathrm{Tay}}=2$は棄却せず、条件付き抽出と解析確率の再結合で扱う。
> ここで決めたのは抽出方法であり、3イベント補正の精度は次のスライドで検証する。

---

### スライド10. $K_{\mathrm{Tay}}=2$：3イベント補正の構造検証

**タイトル**

> $K_{\mathrm{Tay}}=2$でも、3イベント補正の構造残差は最大1.373%に収まった

**調べたいこと**

スライド9の条件付き抽出を使い、$K_{\mathrm{Tay}}=2$のイベント列で、
$\kappa_1$--$\kappa_3$で説明できない4イベント以上の境界効果がどの程度残るかを調べる。

**スライド7--8との違い**

スライド7--8の$K_{\mathrm{Tay}}=0$検証では、局所係数の較正と未使用一体回路に別のイベント列を使う独立検証を行った。
対応あり検証は行っていない。$K_{\mathrm{Tay}}=0$では標本数を増やした独立2シード検証により、
2イベント補正の約8%残差と3イベント補正の改善を分離できた。

$K_{\mathrm{Tay}}=2$では低確率分類を別々に推定するため独立平均の揺らぎが大きく、
初期比較では補正式の構造誤差を判定できなかった。そのため、ここで初めて対応あり構造検証を追加した。

**条件**

スライド9と同じH4、1.0 Å、STO-3G、DFランク12、$L_D=3$、短時間幅0.025、
$K_{\mathrm{Tay}}=2$、$L=4,6$。対応あり検証は、次数0を各長さ100イベント列、
次数2が1か所の条件を各位置25イベント列で行う。

**二つの比較方法と結果**

最初に別標本から求めた係数で比較したが、標本誤差と補正式の構造誤差を分離できなかった。
そこで、補正式の構造だけを調べるため、同一イベント列を使う対応あり検証を追加した。
二つは競合する最終推定法ではなく、後者は構造誤差を切り分けるための検証である。

| 方法 | 比較したもの | 最大点誤差 | 解釈 |
|---|---|---:|---|
| 初期の独立係数検証 | 別標本から求めた局所係数による予測 対 未使用一体回路の平均 | 9.119% | 両平均の標本誤差が混ざり、補正式の構造誤差を切り分けられなかった |
| 対応あり構造検証 | 同一イベント列の局所窓から作った予測 対 その列の一体回路 | 1.373% | イベント列ごとの大きな変動を差分で除き、補正式の構造誤差を切り分けた |

**二つの結果からどう判断したか**

1. 独立係数検証の9.119%は5%を超えたが、係数平均と一体回路平均の標本誤差が混ざっているため、これだけでは3イベント補正の構造を棄却できない。
2. 対応あり検証でその標本変動を除くと、3イベントで説明できない構造残差は最大1.373%、RZ 95%上側診断は1.796%であった。最大z値7.535なので残差は非ゼロだが、大きさは判定基準5%内である。
3. したがって、検証したH4、$L=4,6$、「全次数0」と「次数2が1か所」の範囲では、$K_{\mathrm{Tay}}=2$の基本モデルとして$\kappa_1$--$\kappa_3$を残し、$\kappa_4$は必須としない。
4. この条件での次の課題は、補正の長さを増やすことではなく、独立方式で使う平均係数の推定精度を高めることである。その手順をスライド11で示す。

---

### スライド11. 実際の運用推定器

**タイトル**

> 構造検証を通した式を、実際に使えるコスト推定手順へ落とす

**前スライドからの接続**

スライド10では、3イベントまでの局所式が構造的に5%内で使えることを確認した。次に、その式の係数を
未使用の長い回路を見ずに推定し、大きな系の総コスト探索へ供給する実際の手順を示す。

**フロー**

1. 保存した同一のDFハミルトニアンデータとコンパイル条件を固定する。
2. $K_{\mathrm{Tay}}=2$ではTaylor次数0と2の両方を扱う。次数0の単一イベントは種類数が有限なので全種類を列挙する。
3. 組合せ数の多い次数2を含む局所回路は、Taylor次数の配置を指定して条件付き抽出する。
   次数2は通常抽出ではほぼ現れず、列の先頭・中央・末尾のどこにあるかで、2・3イベント局所窓へ入る回数と境界の作られ方が異なる。
   そのため配置ごとに必ず抽出し、最後に本来の解析確率で重み付けする。
4. 同一／異種DFフラグメント境界に分けて2イベント係数を求める。
5. 局所係数を「係数の長さ」「Taylor次数の配置」「DF境界種別」で分類し、各分類を少数回コンパイルして
   $\kappa_j$のRZ成分の標本分散$s_h^2$を測る。分類例は、$\kappa_1$の次数0／2、$\kappa_2$の次数配置$(0,0),(0,2),(2,0),(2,2)$と
   同一／異種DFフラグメント境界、$\kappa_3$の$(0,0,2)$などの次数3組である。$s_h^2$はモデル誤差ではなく、同じ分類内でイベントを変えたときの係数値のばらつきを表す。
6. 予測式中の期待出現回数$a_h$、予備標本の標準偏差$s_h$、1標本の相対計算量$c_h$から、各分類の本標本数を決める。

   $$
   n_h\propto\frac{|a_h|s_h}{\sqrt{c_h}}
   $$

   $c_h$は実測時間ではなく、その局所係数を1標本求めるためにコンパイルする回路のイベント作用数の合計をほぼ表す。
   つまり、長い列で出現しやすく、係数値のばらつきが大きい分類へ多く配分し、1標本を作るイベント作用数が多い分類では配分を抑える。
7. 保存した$\kappa_1,\kappa_2,\kappa_3$と解析確率だけで長いイベント列の期待コストを予測する。
8. 係数調整に使っていない代表的な長い一体回路で独立検証する。

**実装上の主張**

- 予測段階ではQiskitによる回路構築・コンパイルを行わない。

**再現単位の注意**

別の実行環境で名目上同じ分子からDFを再構築した検証データは最大11.68%、z値5.215ずれたため結合しなかった。
分子条件だけでなく、保存したDFハミルトニアンデータとコンパイル条件を固定する。

---

### スライド12. H4の独立検証と条件移送

**タイトル**

> 1--3イベントモデルはH4の多くの条件で5%内、$L_D=6$だけ不通過

**二つの長さの区別**

- $L_D$：決定論側へ入れるDFフラグメント数。ハミルトニアンの分け方を表す。
- $L$：一つのRTEイベント列に並ぶイベント数。検証するランダム回路の長さを表す。

**主計算**

H4、DFランク12、$L_D=3$、短時間幅0.025、$K_{\mathrm{Tay}}=2$、未使用$L=4,6,8$。

- 全指標最大点誤差2.936%。
- RZ最大z値2.074。
- 予測側95%半幅1.537%。

**移送条件表**

| $L_D$ | 短時間幅 | 最大点誤差 | 判定 |
|---:|---:|---:|:---:|
| 0 | 0.025 | 2.043% | 通過 |
| 3 | 0.020 | 2.140% | 通過 |
| 3 | 0.025 | 3.777% | 通過 |
| 3 | 0.030 | 3.541% | 通過 |
| 6 | 0.025 | 6.119% | 不通過 |

**結論**

> 1--3イベントモデルを全条件へ無条件に移送せず、候補条件ごとに未使用回路で判定する。

---

### スライド13. 不通過条件の4イベント補正

**タイトル**

> $L_D=6$だけ4イベント項を追加し、5%点候補を回復した

**条件**

H4、DFランク12、$L_D=6$、短時間幅0.025、$K_{\mathrm{Tay}}=2$、未使用$L=8$。

**二つの検証の違い**

スライド10と同じ二段階検証を、3イベント補正が不通過だった条件に対し、
4イベント補正まで広げて適用する。

| 検証 | 係数の作り方 | 比較する長い回路 | 主に測るもの |
|---|---|---|---|
| 独立運用検証（固定$\kappa_1$--$\kappa_4$） | 別に抽出した1--4イベントの局所回路から係数を推定し、固定する | 係数較正に使っていない$L=8$回路 | 係数推定誤差・未使用回路の標本誤差・4イベント補正で説明できない差を含む運用上の予測誤差 |
| 対応あり構造検証（$\kappa_1$--$\kappa_4$） | 各$L=8$イベント列から、その列自身の1--4イベント局所窓を作る | 同じイベント列の一体回路 | イベント列ごとの大きな変動を差し引き、5イベント以上にまたがる未説明の境界効果を測る |

**結果**

- 1--3イベントモデル：6.119%で不通過。
- 独立運用検証（固定$\kappa_1$--$\kappa_4$）：最大点誤差3.750%。
- 同一イベント列を使う対応あり構造検証：最大点誤差4.008%、RZ 95%診断4.902%。
- 独立係数評価のRZ 95%上側診断は7.045%で、統計的不確かさは残る。

3.750%と4.008%は異なる標本と異なる誤差の測り方から得た点推定なので、対応あり検証が必ず
小さい値になるわけではない。重要なのは、独立評価では係数推定の不確かさを含む95%診断が7.045%、
対応あり検証では4イベント補正で説明できない残差の95%診断が4.902%だったという役割の違いである。

**結論**

- 4イベント項は構造上有効。
- 最初から全条件に追加せず、3イベントモデルが未使用回路で不通過となった場合だけ使う。
- 4イベント補正を厳密5%保証とは呼ばない。

**強調表示**

`κ1–κ3: 6.119% → κ1–κ4: 約4%`を、二列の比較表として大きく表示する。

---

### スライド14. 制御付き反復回路の$q$依存

**タイトル**

> 制御付き反復の期待回路コストは、検証した$q$範囲で一次式により予測できた

**モデル**

$$
\widehat D_q=a+bq
$$

**条件**

H4、DFランク12、$L_D=3$、$\delta=0.1$、$r=4$、$K_{\mathrm{Tay}}=0$、同一コンパイル条件。

**結果**

- $q=1,2$各300イベント列から未使用$q=4$：全指標最大0.307%。
- $q=1,2$各150イベント列から未使用$q=8$：全指標最大0.0529%。

**評価範囲**

通常の制御付き時間発展部分回路のみ。補助量子ビットのアダマール、測定軸変更、測定、状態準備、
量子測定回数は含めない。

**推奨表**

係数決定に使った$q=1,2$と、未使用検証点$q=4,8$を分け、標本数と最大誤差を表で示す。

---

### スライド15. H5の系サイズ方向・独立検証
**タイトル**

> H5でも3イベントモデルが未使用回路の5%点基準を通過した

**条件**

H5鎖、1.0 Å、STO-3G、10量子ビット、プロジェクト設定DFランク9、$L_D=4$、短時間幅0.025、
$K_{\mathrm{Tay}}=2$、$L=4,6,8$、同一コンパイル条件。

**この設定の理由**

- H4だけで得たモデルを、次に大きく、なお一体回路の独立検証が可能なH5へ移して系サイズ方向を確認する。
- 距離、基底、短時間幅、Taylor打切り次数、コンパイル条件はH4の主検証とそろえ、系サイズ以外の違いを抑える。
- DFランク9はプロジェクト設定から得たH5の保存DF表現である。$L_D=4$は全分割の最適化で
  選んだ値ではなく、この系サイズ検証で固定した一つの中間分割である。
- $L=4,6,8$はH4と同じ長さを使い、短い列から長い列へ予測できるかを比較するために選んだ。

**左パネル：同一イベント列を使う対応あり構造検証**

- すべてTaylor次数0を各長さ100、次数2が1か所の条件を各位置20。
- 1--3イベント：最大点誤差1.665%。
- 1--4イベント：最大点誤差0.551%。
- 5%を満たす最小局所長として3イベントを選択。

**右パネル：独立した係数較正・未使用回路検証**

- 係数較正で実現した最大RZ相対標準誤差：0.745%。
- 未使用回路：すべてTaylor次数0を各長さ500、次数2が1か所の条件を各位置125。
- 最大点誤差3.776%。
- 最大予測側95%半幅1.459%。
- 最大z値2.009。
- 95%上側診断7.461%は硬い判定ではない。

**標本数とTaylor次数条件の理由**

- 対応あり構造検証は、同じイベント列で差を取るため列ごとの大きな変動が消えやすい。
  そこで構造の確認には、次数0を各長さ100、次数2を各位置20という比較的小さい標本数を使った。
- 次数2は自然抽出ではほぼ現れないため、「1か所だけ次数2」と位置まで指定し、列の先頭・中央・末尾を漏れなく確認した。
- 独立検証は別々に求めた係数平均と一体回路平均を比較するため標本誤差が大きくなりやすく、
  未使用回路を次数0で各長さ500、次数2で各位置125まで増やした。
- これらの標本数は実行時間と統計精度の釣り合いから定めた。
  理論から一意に決まる値ではなく、計算量と検証精度のための暫定設定である。

**結論**

スライド10で説明したとおり、左は採用したイベント数より長い範囲にまたがる未説明の境界効果、
右は係数推定誤差と未使用回路の標本誤差も含む実運用全体の予測誤差を測る。

> H5のこの保存ハミルトニアンデータ・コンパイル条件では、運用推定法として1--3イベントモデルを受理する。

**参照データ**

- `artifacts/rte_cost_system_size_h5/2026-08-25/outputs/h5_sto3g_d100_rank9_ld4_s0025_k2_paired_l4_l6_l8_zero100_single20_v1.json`
- `artifacts/rte_cost_system_size_h5_independent/2026-08-26/outputs/h5_sto3g_d100_rank9_ld4_s0025_k2_independent_l4_l6_l8_zero500_single125_v2.json`

---

### スライド16. 現在の採用規則と次工程

**タイトル**

> 条件ごとに3イベントモデルを較正し、不通過時だけ4イベントへ進む

**採用フロー**

```text
保存ハミルトニアンデータ・コンパイル条件を固定
        ↓
κ1–κ3を局所回路で較正
        ↓
代表的な未使用一体回路で独立検証
        ↓
5%点誤差・2%予測精度を通過？
   ├─ 通過   → 採用し、以後の予測ではコンパイルしない
   └─ 不通過 → κ4または追加の境界分類を検証
```

**現在の結論**

- イベント単純加算は不採用。
- H4・H5の明示条件で局所境界補正モデルを支持。
- 検証済み範囲のモデル不確かさとして約5%を持たせる。
- 候補の推定回路コスト差が約5%以下なら、追加抽出または一体回路の直接コンパイルで確認。
- 回路コスト検証の広い予備検証は一旦区切る。

**次工程**

- 次の主工程は、今回の回路コスト推定をRPE各段の必要測定回数$N_{m,b}$へ接続すること。
- その中で、位相誤差予算$\beta$と失敗確率$\alpha$の配分を変え、測定回数と総コストへの影響を比較する。
- 配分と各段の$(r_m,K_m)$を接続した後、$L_D$・$\delta_{\mathrm{time}}$の外側候補を総コストで比較する。
- 実際に必要なイベント列長が8を超える場合だけ、最大使用長付近の独立検証を追加する。


**最後の留保**

> 部分ランダム化の最終総コストおよび決定論PFに対する優位性はまだ評価していない。

## 6. 数値と根拠データの対応

| スライド | 数値 | 主な根拠データ |
|---:|---|---|
| 5 | イベント単純加算48--58%、3部分和0.959% | `artifacts/random_circuit_cost_validation/` |
| 7 | Taylor次数0の2・3イベント補正、$L=8$ | `artifacts/rte_boundary_cost_validation/`、`artifacts/hierarchical_cost_validation/` |
| 8 | 同一／異種DFフラグメント分類0.849% | `artifacts/rte_boundary_pair_validation/` |
| 9--10 | Taylor次数2の低確率イベント、9.119%、1.373% | `artifacts/rte_order_stratified_cost_validation/` |
| 12 | H4の運用検証・条件移送 | `artifacts/rte_connected_cluster_cost_validation/` |
| 13 | H4の4イベント補正 | `artifacts/rte_cost_followup_batch/2026-08-25/`、`artifacts/rte_cost_paired_k4_l8/2026-08-25/` |
| 14 | 制御付き反復$q=4,8$ | `artifacts/hierarchical_cost_validation/`、`artifacts/rte_cost_data_batch/2026-08-25/` |
| 15 | H5の対応あり構造検証・独立検証 | `artifacts/rte_cost_system_size_h5/2026-08-25/`、`artifacts/rte_cost_system_size_h5_independent/2026-08-26/` |

## 7. 作成時に避ける表現

- 「ランダム回路コストを厳密に求められた」
- 「5%以内が保証された」
- 「H12でも同じ係数が使える」
- 「モンテカルロ誤差を排除した」
- 「大きな系で長い一体回路をすべてコンパイルする」
- 「z値が大きいのでモデルは失敗」
- 「95%上側診断が5%を超えたので、事前基準を不通過」
- 「回路コストの検証から最終総コストが得られた」

代わりに、`指定条件の未使用回路による独立検証`、`点誤差基準`、`予測精度`、`診断量`、
`条件ごとの再較正`、`最終総コストは未評価`という表現を用いる。
