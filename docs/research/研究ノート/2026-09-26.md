# 研究ノート 2026-09-26

## P-D S0契約

P-D現実化gate後の研究再設計として、主RQを特定PFの勝敗ではなく、energy/phase errorと
random-tail burdenを同時に見たPF選択modelの十分条件・破綻条件へ置いた。固定H4、有限5公式、
nested/native構成、B0/B1a/B1b/B2/B4、共通時間`T=0.8`、総位相誤差`8e-7 rad`、regret 5%、
S1後の強制停止を結果前に文書化した。

## S1実行

primary 300 taskをv1 expectedへ固定した。最初の実行は、高段PFでtail occurrence数が`R=16`を
上回る点をinteger-allocation infeasibleとして保存せず例外にしたため、result生成前に停止した。
grid、閾値、K4 trigger、境界規則、分類規則は変更せず、配分不能点を明示的infeasibleとして残す
修正だけを行った。v1 expectedは技術的失敗履歴として保持し、source hashをv2へ非上書きで再固定した。

v2は1.369秒で完了した。primary K2は境界追加後308候補、B4 feasibleは87件だった。

- nested B1b/B2/B4: new fourth、`delta=0.2,m_D=16,R=16`
- native B1b/B2/B4: new fourth、`delta=0.2,R=16`
- combinedもnativeの同設定
- B2のB4 regret: 全scopeで0
- 限定K4: formula、delta、m_D、Rの変更なし

B0はfalse acceptanceまたは大regretとなった。outer-stageだけのB1aはnested/combinedで
`delta=0.4,m_D=128,R=16`を選び、finite-infeasibleだった。一段延長後もm_D上限に達したため、
一次分類はCase Bだが`undetermined_boundary`を付け、GO判定を出さなかった。Case Bを生じさせたのは
B1aだけで、one-shot workを含むB1bはB2/B4と一致した。

## 判断

finite補正がPF選択を変えるCase C、またはnested/nativeでformulaが変わるCase Dの証拠は得られなかった。
事前登録どおりS1で停止し、S2、H12、長RPE、compiled total costへ進まない。

次は計算ではなく、B1aを研究baselineに残す意味、B1bで既知modelが十分と言える範囲、Case Bに
既知研究を超える適用条件があるかを再検討する。追加境界計算を行う場合は別の事前登録を要求する。

## 識別子

- v1 expected: `c1e5631e547c68670c74244109817c3640216d1a55955d293d0f58bdbd172ff9`
- v2 expected: `e3eacbb9d8928f781df7709208f048c59aaab7052304e4eae4adc346bbf6d0d5`
- result: `0ba7764da7b7d8b7e195a5c315d3dc0a65c2c79ce01c51cf021a2685977487d2`
- result file SHA-256: `6b8de6e255eb0796d93398c767017c2899837a63c7230e9956fb2d9beedbeaec`
- 専用test: `5 passed`
- 全suite: `611 passed, 2 skipped, 4 warnings`、失敗0


## S1固定artifactの事後再解析

外部reviewを受け、再解析条件を先に`pd_s1_posthoc_reanalysis_plan.md`へ記録した。S1 v2の308候補、
regret 5%、feasibility、一次A--D分類を変更せず、B1b/B2/B4、B4最良から5%以内、B1aの`m_D`列、
nested/native work内訳を再集計した。新しいHamiltonian、対角化、RTE sampling、compileは実行していない。

B1b/B2/B4は全scopeで同じ候補、false acceptanceなし、B4 regret 0だった。5%近傍はnested 5候補、
native/combined各1候補で、B2 objective相対誤差は最大0.03132%。一方、全候補ではB2 proxy受理/
B4不適格がcombined 45件あり、B2の全域精度は主張しない。

一次`Case B + undetermined_boundary`は保存し、主baselineだけを
`A_equivalent_on_frozen_candidate_set_under_B4`と事後解釈した。B1aの固定tailは理想unit radiusでも
finite位相上界`1.8178593e-6 rad`が予算`8e-7 rad`を超え、`m_D`追加だけではB4適格化できない。

nested/nativeのB4 objective比13.2297は主にdeterministic action差で、compiled回路優位性ではない。
P-DはS2へ進めず停止する。次はR3の先行研究差分と最小研究契約を検討するが、R3の採用・新規性は
まだ確定していない。

- post-hoc fingerprint: `976212ee45a472bf0091064e8baf3eb7a861f2c240cdcfda445e64b4c72e0245`
- post-hoc file SHA-256: `d8657c52e609e524c4e43a3004948ffb3fd54e6f60219442638b847ade3e699b`
- 専用test: `4 passed`
- 全suite: `615 passed, 2 skipped, 4 warnings`、失敗0


## R3先行研究監査と条件付き最小契約

P-D S1事後再解析の停止判断を受け、partial randomization、Hamiltonian partition error/cost、
perturbative error estimator、circuit-aware term allocation、SPRINT/GRADE、および一般の
multi-fidelity optimization / best-arm identificationをscopedに監査した。

fragment norm、PF誤差、per-step cost、randomized remainderを合わせて最安の分割・公式を選ぶ広いR3は、
既存研究との重複が強く独立主題としてNo-Goとした。現行RQ3は将来の総resource評価に必要な工学的目的
として保持するが、その達成だけを論文上の新規性とはしない。

条件付きで残すR3-Sは、ordered DF prefix固有のcheap diagnosticにより、高価な共通詳細参照と同じ選択を
認証するか棄却し、未使用条件で5% regretと詳細評価数を同時に評価する問いへ狭めた。ただし一般の
multi-fidelity best-arm identificationとも重なるため、quantum-specificな構造・SPRINTとの差・partial-RTE
固有情報の三点を一文で示すR3-S0を先に要求する。

R3-S0を通らなければ`STOP_R3_NO_METHOD_DELTA`とし、数値pilotを開始しない。通った場合だけ、
high-fidelity評価数をexhaustive比50%以下、certified decisionのregret 5%以下、false acceptanceなし、
事前規則どおりのabstentionを要求する条件付きR3-S1契約を別のsource-hash付き事前登録へ落とす。
既存H4 prefixは広く参照済みなので、新しいblind evidenceとは呼ばない。H12、長RPE、full compileは保留する。

## R3-S0完了・R3停止

一般方法論側を追加監査した。certified multi-fidelity zeroth-order optimizationは異なるcostの近似評価から
最適化し、data-drivenなoptimization-error上界まで返す。multi-fidelity best-arm identificationも、
低精度・低cost評価を選択しながら高精度best armをfixed confidenceで同定するcost complexityを既に扱う。
quantum simulation側にも、摂動的なrigorous error boundを効率的な古典cost functionへ変えてsequenceを
事前最適化する方法がある。

現行資産には、cheap/high-fidelity差をprefix横断で拘束するquantum-specificな新しい上界がない。
S1全候補ではB2受理/B4不適格45件、objective差最大約77.15%であり、compiled-cost proxyにも
$L_D=6$等の既知破綻がある。$\lambda_R$、tail時間、finite cutoff、integer allocationは特徴量だが、
certified selectionへ結ぶ新規な関係は得ていない。

R3-S0の三GO条件を全て不通過とし、`STOP_R3_NO_METHOD_DELTA`でR3を停止した。R3-S1、追加$L_D$、
H12、長RPE、full compileは開始しない。RQ3は将来の工学的resource評価目的として残すが、独立研究の
新規性とはしない。R6/R8へは自動移行せず、次は新しいRQを先行研究差分から再設計する。

## finite-RTE phase/radius再設計 FR-0

P-D S2とR3を停止した後の新しいRQ候補として、有限paired-Taylor RTEの打切り誤差を
Hadamard複素信号の位相方向と信号半径方向へ分ける案を監査した。FR-0は計算ではなく、
数式、先行研究、比較baseline、次の最小検証を固定する段階とした。

重要な規約修正として、各有限RTE occurrenceをTaylor numeratorで置換した
normalization-corrected演算子$A_{\rm corr}$と、実際のsampled-unitary平均
$A_{\rm mean}=A_{\rm corr}/\mathcal B$を分けた。位相は両者で同じだが、観測信号半径には
$1/\mathcal B$が掛かる。fresh independent samplingを積平均の仮定とし、同じtrajectoryを反復利用する
相関samplingには適用しない。

相対誤差$D_j=U_j^\dagger A_j-I=F_j+iG_j$を用い、一次Hermitian成分、一次anti-Hermitian成分、
二次以上の積剰余、参照信号半径から、非可換積の位相上界と信号半径下界を導出した。
真の信号半径を使う参照版と、事前に与えた$\underline\rho$だけを使う利用可能情報版を分離した。
比較対象は既存Taylor remainderの`OLD-NORM`、spectral supremumを使う`STRONG-NORM`、可換
`SCALAR/EIGEN`とした。

finite LCU/RTE、unitary PFのspectral解析、randomized平均信号、classical phase-lag/dissipation、
QPEの状態依存誤差、一般nonunitary dynamicsのAmplitude-Phase Separationを式の対象ごとに比較した。
限定監査では同じ仮定・同じ出力の結果を確認できなかったが、不在や世界初は主張しない。
また既存研究との名称衝突を避け、この研究で`APS`を手法名・略称に使わない。

FR-1は2×2 toyだけを使い、可換/非可換、K=0/2/4、正負時間、対称/非対称配置、identity relative
phaseを固定gridで検査する。主判定は解析的に$\underline\rho=0.8$を保証できる状態とし、
密行列から読んだ真の$\rho$だけで得る改善をGO根拠にしない。判断は
`FR0_COMPLETE_GO_FR1_MECHANISM_ONLY`であり、FR-1以降の数値計算、library/runner/test、artifact、
manifest更新はまだ行っていない。FR-1後に必ず停止して再判断する。

## finite-RTE phase/radius FR-1

事前登録した33条件・99状態の2×2 fixed gridを変更せず実行した。G0 semantic、G1 soundness、
G3 conditioning、G4負時間/K4/非対称移送は通過し、495個の適用可能method recordでbound違反は0だった。

主判定の$\underline\rho=0.8$では提案/STRONG位相上界比が$q=1,2,4,8,16$で
1.106794、0.855074、0.727864、0.663982、0.631998となり、50%基準を通らなかった。
$q\ge4$では両法が既に`1e-3 rad`以下なので片側認証triggerも発火せず、G2は不通過だった。

参照固有状態またはdense真値では$q=2,4,8,16$で50%以下の改善が出たため、事前規則どおり
`GO_FR2_MECHANISM_ONLY`とした。これは位相・半径分離機構の確認であり、利用可能情報による
実用的認証ではない。FR-2、H4/H12、compile、RPE総costへ進まず停止する。続けるなら、大規模計算でなく
高い$\rho$をoracleなしで認証する条件、または$\kappa s$項を構造から縮める境界に独立差分があるかを
先に再設計・監査する。

- fingerprint：`d96b200163f3a432652656ae97c65c837e01fe81323cd69528373dff16ce6152`
- file SHA-256：`6a81a0ba6e39ba0f5d79ba026a2a45c3c65709e071e9c6fa5606b3e99a89b0f7`
- 専用test：`4 passed`
- 全suite：`619 passed, 2 skipped, 4 warnings`、失敗0
- H4/H12、compile、Monte Carlo、最終cost：0件

実行前の完全なFR-1事前登録SHA-256は
`bc8066d7a31a3f46f476d2c591c7f0dad5e6f49023fc261dc518b61e2ec600a3`。凍結親FR-0契約は後の監査で
Markdown末尾の切断が判明したため不変履歴として残し、現行契約で修復した。完全なFR-1事前登録が
独立にgrid・gateを保持しており、計算と判定への影響はない。

## FR-R0正scalar分離・構造比較契約の採択

FR-1の`GO_FR2_MECHANISM_ONLY`を受けた再設計案を監査し、正scalar分離、I0/I1/I2情報層、
強いnorm baseline、非一様4×4の判定条件を[FR-R0正式契約](../fr_revision_scalar_structure_contract.md)へ固定した。
旧FR-1のG2不通過と停止判断は変更しない。$h^2=I$のK=2 numeratorは正scalar×unitaryへ厳密に分かれ、
旧比が$\underline\rho=0.8$で0.6へ近づくため、旧toyだけの改善は独立主張に使わない。

FR-R0と数値事前登録を分離した。次は既存33条件のFR-R1aをposthocとして固定し、その値で閾値を
調整せずに、FR-R1bのsequence、同一状態の全$q$再利用、controls、位相予算、K/r候補、GO/STOPを
別文書へ事前登録する。現時点では実装・計算・test・artifact生成を行っていない。

## FR-R1a事後計画・FR-R1b事前登録の分離固定

FR-R0の次段を二文書へ分離した。FR-R1aは既存FR-1の33条件・99状態だけを使うposthoc説明監査で、
旧G2不通過と`GO_FR2_MECHANISM_ONLY`を変更しない。FR-R1bは非一様4×4の20 matrix conditions、
61 state rows、2 semantic controls、固定状態、位相予算、物理半径基準、R0--R7を結果前に固定した。

FR-R1aとFR-R1bの共通位相予算は`1e-2,1e-3,1e-4 rad`とし、FR-R1a結果の間へ予算を後付けしない。
FR-R1bの主状態は同一条件で一度だけ構成し、全qで再利用する。真のrhoは入力certificate監査だけに使い、
境界入力は0.8のままとする。現時点では両方とも未実行で、次はFR-R1aだけを実装・実行する。

## FR-R1a正scalar事後再解析の完了

凍結計画どおり、旧FR-1 result、凍結事前登録、計画hashを先に照合し、33条件・99状態を決定論的に
再構成した。state fingerprintは99/99一致し、旧recordとの最大数値差は0。正scalarを含む9手法の
891 method recordで位相上界・物理半径下界のsoundness違反は0だった。

主5行の全てで`SCALAR-NORM-COMMON`が`SCALAR-FR-COMMON`より厳しく、固定位相予算
`1e-2,1e-3,1e-4 rad`と物理半径下限0.2では、共通scalar比較・各法最適化比較ともFRだけの
片側認証は0だった。I2 oracleだけの片側認証も0で、凍結分類に従い
`POSTHOC_SCALAR_EXPLAINS_OLD_GAIN`とした。

これはinvolution toyの事後説明監査であり、旧G2不通過と`GO_FR2_MECHANISM_ONLY`は変更しない。
新しい研究GOを認めず、FR-R1bのgrid・閾値・状態規約・R0--R7も変更していない。FR-R1b、H4/H12、
compile、Monte Carlo、RPE総costは未開始のままである。

- result fingerprint：`5cc5c29656b9ceb69a00674cee5987b93c7cc8d9403892c57b341a51c878a18a`
- result file SHA-256：`3fab6acdde3798dc7005101713ecaa708f5cf0b5fc24d8c77f80939dcaa6a610`
- expected fingerprint：`83fa10248e71cad40fa0ba87877c49f88f6755c24470c232fb41ccff4205f4a9`
- 専用test：`4 passed`
- 元FR-1との関連test：`8 passed`
- 全suite：`623 passed, 2 skipped, 4 warnings`、失敗0
