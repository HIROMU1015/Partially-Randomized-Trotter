# 2026-09-22 WP03 PF係数選択感度

## 記録情報

| 項目 | 内容 |
|---|---|
| 日付・時刻 | 2026-09-22 00:17 JST |
| 基準commit | `efa90e0b14a95c296b305bfeea37867e56c83a0d` + dirty worktree |
| branch | `main` |
| 対象scope | Gate S1のWP03。PF係数だけを変更した$L_D$・$\delta$選択感度 |
| 状態 | local検証完了／外部再現・decision-grade比較は未実施 |

## 目的

WP04までの同じH4 task、cost provider、配分、schedule選択を固定し、$C_D$、論文D6、
支配固有位相係数だけを差し替えたときに、最適$L_D$、$\delta$、candidate regretが変わるかを
確認する。係数の数値差自体でなく、研究方向の選択への伝播を判定する。

## 固定条件

- H4 linear chain、1.0 Å、STO-3G、8 qubit、DF rank 12
- snapshot hash `56e4df83655aa2f2f8132126f2635996516dc2cfb1bf4cb8d95490184ad631e5`
- 係数監査は$L_D=0,3,12$、cost比較はWP01-Sでcost証拠のある$L_D=3,12$
- CA/10、$\delta=0.01,0.0125,0.02$
- WP04のround別compiled-RZ schedule、cost感度重み$\alpha$、候補別$\beta$配分
- WP01-Sの$q=1,2$状態準備なしHadamard較正を再利用し、$q>2$はaffine外挿

$L_D=0$はWP01-Sの拡張成分作用数proxyでscreen out済みで、同じscopeのcompiled-cost証拠を
作っていないため、係数監査だけに残した。

## 係数監査

| $L_D$ | $C_D$ | 論文D6 | 支配固有位相 | $C_D$/D6差 | D6/固有位相差 |
|---:|---:|---:|---:|---:|---:|
| 0 | 0 | 0.0115338 | 0.0114931 | $-100\%$ | $+0.354\%$ |
| 3 | 0.0117236 | 0.0133991 | 0.0133569 | $-12.50\%$ | $+0.316\%$ |
| 12 | 0.0134411 | 0.0134257 | 0.0133833 | $+0.115\%$ | $+0.317\%$ |

$C_D$もD6・支配固有位相と同じconditioned窓$\delta=0.05,0.1,0.2,0.4$で再fitした。既存の
0.01--0.08 fit値ではなく、このcommon-window値をWP03の選択感度へ使用した。
$L_D=0,3$で$C_D$がfull partial係数を大きく過小評価するため、$C_D$は広いscreening専用とし、
shortlist後にD6を候補ごとに計算する方針を維持する。D6は各候補の6点中4点が
$|\sin(E_0\delta)|\geq0.1$を満たし、$\delta=0.0125,0.025$はfitから除外されている。

D6のstored signed biasは負、$C_D$と支配固有位相は正だった。これは符号規約が直接揃って
いないため、絶対値係数を比較した。交換子項別の分解はartifactにないので、符号差を相殺と呼ばない。

## 選択・regret結果

3係数×2候補×3$\delta$の18条件は全てPF予算とround実行可能性を満たした。全係数policyで
点推定最良は$L_D=12,\delta=0.02$、RZ $1.6963\times10^{12}$だった。係数policyを跨いだ
selection regretは全て0で、係数選択による$L_D$・$\delta$変更はなかった。

D6での候補regretは次の通りである。

| 候補 | $\delta$ | 点regret |
|---|---:|---:|
| $L_D=3$ | 0.01 | 37.70% |
| $L_D=3$ | 0.0125 | 74.49% |
| $L_D=3$ | 0.02 | 5.216% |
| $L_D=12$ | 0.01 | 100.00% |
| $L_D=12$ | 0.0125 | 100.00% |
| $L_D=12$ | 0.02 | 0% |

ただし$L_D=3,\delta=0.02$対$L_D=12,\delta=0.02$の相対差scenarioは、local 5%＋較正で
$[-15.91\%,28.57\%]$、25%移送＋較正で$[-46.20\%,90.91\%]$となり0を跨ぐ。点regret
5.216%は決定論endpointの確定優位性ではなく、方向判定は引き続き`undetermined`である。

$\delta=0.02$でPF予算外になるまでのD6係数増加余裕は$L_D=3$で6.76%、$L_D=12$で6.55%だった。
costed候補のD6対支配固有位相差0.317%はこの余裕より小さい。一方、この余裕を別instanceへ
移送せず、係数が約6.6%以上変わる場合はround horizonと$\delta$を再評価する。

## 実行コマンドと検査

```bash
.venv311/bin/python scripts/run_research_direction_pf_sensitivity.py
.venv311/bin/python -m pytest -q tests/test_research_direction_pf_sensitivity.py
```

専用testは`4 passed`。変更後のlocal全suiteは`507 passed, 4 warnings`だった。warningは既存の
grouped-UWC testにおけるcomplex-to-real cast由来であり、今回の検証失敗ではない。manifest、
artifact validator、生成元source hash、144項目のカタログ構造、JSON、`git diff --check`も通過した。

## 成果物

- `artifacts/research_direction_pf_sensitivity/2026-09-22/`
  `wp03_pf_coefficient_selection_sensitivity_v1.json`
- content fingerprint `2faa8d63...e4ff3`

成果物はlocal dirty-worktree evidenceであり、immutable CI、外部再現、最終総costまたは
decision-grade比較ではない。

## 次に行うこと

Gate S1に必要なWP01-S、WP02、WP04、WP03が揃った。追加検証へ進む前に、partial候補と
決定論endpointの区間、改善の主因、係数感度、最大の残存不確かさを統合して研究方向を判断する。

# 2026-09-22 Gate S1研究方向判断

## 判断と理由

WP00、WP02、WP01-S、WP04、WP03のfingerprint済み成果物を、新しい物理計算や回路compileを
行わずに統合した。WP04の決定論endpointは点推定で4.958%低いが、local 5%とtransfer 25%の
両scenarioが重なるため、「同点」でなく`undetermined_not_tied`とした。最大の共通利得は
$\beta$、次いで$\alpha$再配分であり、$L_D=3$のpartial固有round scheduleは固定schedule比
1.93%に留まる。3つのPF係数policyは全て$L_D=12,\delta=0.02$を選び、係数選択も未決定を
解消しなかった。

従って、最大の残存不確かさはPF係数、finite-RTE、統計配分でなく、full controlled
interrogationの回路scope・構造と判断した。研究方向はT4（長いランダム回路cost予測）とT7
（信頼できる資源評価・否定的結果）を主軸、T1/T2/T5/T6を限定継続、T3（高次PF）を保留とした。

## 次の一件

次はWP06-aだけを行い、その後にWP05へ進む。代表的な一つまたは二つのDF Z/ZZ eventと短い列で、
support限定basis変換、basis融合、既知scalar/phase、control最適化を一変更ずつ検査する。
controlled branchのrelative phaseを含む小行列同値性を必須とする。

このpilot専用の$\eta_{decision}$は、現候補の4.958%点推定差と同程度のRZ相対変化5%に事前固定した。
5%以上の変化、候補順位反転、$q$傾き・proxy domainの変更、または必要なcontrolled relative phase
補償の欠落があればWP05前に構造とproxyを更新する。該当しなければ残差を不確かさとして持ち、
現構造でWP05へ進む。この5%はWP06-aの研究経路判断専用で、普遍的保証ではない。

## 成果物と状態

- `artifacts/research_direction_gate_s1/2026-09-22/gate_s1_research_direction_decision_v1.json`
- content fingerprint `68f7229d...5c2eb`
- 専用test `4 passed`
- 変更後のlocal全suite `511 passed, 4 warnings`（既存grouped-UWC test由来）

研究経路の判断記録は完了したが、状態準備、未使用長$q$ holdout、full controlled scope、実backend、
noise、最終総cost、優位性判定、immutable CIまたは外部再現は未実施である。

# 2026-09-22 WP06-a回路構造pilot

## 実行条件

Gate S1で事前固定したRZ相対変化5%のtriggerを使い、同じH4、1.0 Å、STO-3G、rank 12、
$L_D=3$ snapshotから係数最大のZとZZを各一つ選んだ。full Gaussian basisと必要なunitary列だけを
保持するsupport限定completion、同一basis融合、whole-event対diagonal-only control、constant・
抽出identityのrelative phaseを比較した。compilerはQiskit 1.3.0、`rz,sx,x,cx`、optimization
level 1、seed 17、coupling mapなしである。

## 結果と判断

support限定は単一controlled ZでRZ 204→78（61.76%減）、ZZで311→189（39.23%減）となり、
5% triggerが発火した。最大operator残差は$4.73\times10^{-15}$だった。ただし同じfragmentの
異なるZZ supportを並べると、長さ2は332→267（19.58%減）だがdepthは45.26%増え、長さ3は
353→406（15.01%増）、depthは86.64%増となった。full basis共有とsupport限定の優劣は列依存である。

whole-event controlから現行のdiagonal-only controlへの変更は同値性を保ち、RZ 2,567→311、
CX 1,834→98となった。constantと抽出identityの合計phaseは$-0.0477143$ radで、controlled補償を
省くとoperator差0.0477098が生じたが、現行補償は参照と一致した。従ってcontrol・phase方針は維持する。

support限定への一律置換は採用しない。次はfocused WP06-bとして、production builderにfull/support
候補を接続し、fragment・support runを見たsequence-aware選択、物理event分布での複数metric較正、
未使用短列holdoutを行う。既存full-basis proxyの高統計化とWP05はこの再較正まで待つ。T4を高優先、
T6をfocused follow-upとして継続する。

## 成果物

- `artifacts/research_direction_structure_pilot/2026-09-22/wp06a_circuit_structure_pilot_v1.json`
- content fingerprint `5faa9b59...7ac5f`
- 専用test `4 passed`
- 変更後のlocal全suite `515 passed, 4 warnings`（既存grouped-UWC test由来）

これは1 snapshot・2代表event・長さ1--3・1 compilerのlocal構造pilotであり、物理event分布の期待値、
$L_D=3$対12の順位、RPE $q$傾き、full partial-$S_2$、Hadamard wrapper、最終総cost、外部再現を
含まない。

# 2026-09-22 WP06-b sequence-aware basis policy

## 実行条件

WP06-aのtriggerを受け、同じH4、1.0 Å、STO-3G、rank 12、$L_D=3$、$\delta=0.02,K=2$、
Qiskit 1.3.0 compilerで物理event列を比較した。元DF basisの最大連続runを単位とし、run長1、2、3
以下または全runをsupport限定へ置換する候補と、full basis共有を比較した。列長1,2,4の各8本を
training、未使用列長3,6の各12本をholdout、$r=1,2,4,8,16,32$の各8本をproxy移送に使い、
master seedを分離した。

production RTE builderには監査可能な明示basis planを渡す経路を追加したが、既定のfull-basis
policyは変えていない。trainingでは各trajectoryのRZ悪化を5%以内に制限し、pooled RZ、CX、
total depthの順で選択した。

## 結果と判断

`support_run_le_1`だけがfull共有以外で5% guardを通過し、RZ合計を11,944から9,590へ減らしたため
固定した。run長2以下の候補は一trajectoryでRZ +7.90%、support-allは最大+99.78%となり棄却した。

固定policyの独立holdoutはRZ -10.67%、CX -8.13%、total depth -2.25%で、trajectory別oracleとの
RZ regretはfull基準0.390%だった。sampled列と強制$K=2$非零phase eventでcontrolled演算子を比較し、
最大残差$1.34\times10^{-15}$、強制eventのrelative phaseは全候補で$3\pi$と一致した。

中央RTE差を既存Hadamard proxyの$q$ slopeへ加えるadditive bridgeでは、RZ slopeは$r=1$で
-2.56%、2で-1.28%、4で-1.04%、8で-3.52%、16で-5.76%、32で-8.00%変化した。WP04の
round、shot、$\alpha$、wrapper interceptを固定した感度bridgeでは、$L_D=3$の点推定が
$1.7848\times10^{12}$から$1.6503\times10^{12}$へ下がり、$L_D=12$の
$1.6963\times10^{12}$より低くなった。ただしlocal区間は重なるため、方向判定は未決定のままである。

次は`support_run_le_1`を明示入力としてWP05へ進み、complete controlled partial-$S_2$とHadamard
wrapperを直接transpileする。今回のadditive bridgeを確定proxyへ昇格せず、$\alpha$・shot数も
WP05後に再評価する。

## 成果物

- `artifacts/research_direction_sequence_policy/2026-09-22/wp06b_sequence_policy_proxy_bridge_v1.json`
- content fingerprint `6b9ec62e...f31a`
- 専用test `5 passed`
- 変更後のlocal全suite `520 passed, 4 warnings`（既存grouped-UWC test由来）

これは1 snapshot・1 compilerのlocal dirty-worktree evidenceである。full wrapper、状態準備、
実backend、noise、量子shot、最終総cost、科学的優位性、immutable CIまたは外部再現を含まない。

# 2026-09-22 WP05-a full controlled interrogation接続

## 実行条件

同じH4、1.0 Å、STO-3G、rank 12 snapshotを使い、$L_D=3$の`support_run_le_1`とfull basis、
tail-free $L_D=12$を比較した。$\delta=0.02,K=2$、$r=1,2,4,8,16,32$、$q=1,2,4$、
各randomized点8 trajectory、Qiskit 1.3.0、`rz,sx,x,cx`、optimization level 1、seed 17、
coupling mapなしである。$q=1,2$を較正、独立seedの$q=4$をholdoutとした。

partial-$S_2$ step/repeated requestへ明示basis planを追加し、選択policyをcomplete controlled evolution、
cosine/sine Hadamard wrapper、ancilla測定まで伝播した。planなしの既定full-basis経路と旧fingerprintは
維持した。

## 結果と判断

選択policyの$q=4$ RZ予測誤差は全$r$・両軸で最大2.288%、全6 metric最大2.431%となった。
WP06-bの中央RTE additive predictionと完全wrapperの直接full/policy差のRZ残差は、full-wrapper平均比
最大2.625%で、いずれも5%基準を通過した。評価した$(r,q)$で直接RZはfull basisより0.99--8.76%
少なかった。H4固定ランダム状態への作用差は最大$1.08\times10^{-16}$、relative phaseも一致した。
小系専用testでは完全operator同値性を確認した。

WP04のround、shot、$\alpha$を固定して新しい$q=1,2$ affine式を長$q$へ移した感度では、
$L_D=3$が$1.5933\times10^{12}$、$L_D=12$が$1.6963\times10^{12}$となり、点では前者が6.07%
低い。ただしlocal区間は重なる。$q>4$の直接回路、$\delta=0.01$、配分再最適化が未実施なので、
この順位をdecision-gradeまたは科学的優位性とはしない。

次はWP05-bとして未使用$q=8$と比較対照$\delta=0.01$を検証する。そこまで通過した後に
WP01-D/C07で$\alpha$・shot数を再最適化する。production既定値はfull basisのままとする。

## 成果物

- `artifacts/research_direction_full_scope/2026-09-22/wp05a_full_controlled_interrogation_connection_v1.json`
- content fingerprint `d8196ef1...f512`
- 専用test `4 passed`
- 変更後のlocal全suite `524 passed, 4 warnings`（既存grouped-UWC test由来）

これはlocal dirty-worktree evidenceであり、状態準備、backend、noise、量子shot、最終総cost、
immutable CIまたは外部再現を含まない。

# 2026-09-22 WP05-b full-scope拡張

## 実行条件

WP05-aと同じH4、1.0 Å、STO-3G、8 qubit、rank 12 snapshot、Qiskit 1.3.0、
`rz,sx,x,cx`、optimization level 1、seed 17、coupling mapなしを維持した。$L_D=3$の
`support_run_le_1`とfull basisを、$\delta=0.02$では未使用$q=8$、比較対照$\delta=0.01$では
$q=1,2,4,8$へ拡張した。各randomized点は8 trajectoryとし、$r=1,2,4,8,16,32$を評価した。

## 結果と判断

$\delta=0.01$は選択policyの$q=8$ RZ誤差最大2.650%、全metric最大2.804%、full basis RZ誤差
最大4.321%、全metric最大4.500%で5%基準を通過した。$\delta=0.01,q=4$の選択policy RZ誤差は
最大2.235%だった。$\delta=0.02,r=32,q=8$だけは、選択policy RZ 5.084%、全metric 5.392%、
full basis RZ 8.995%となり、初回成果物の総合判定は不通過とした。選択policyのずれは合成標準誤差の
約1.02倍、full basisは約1.34倍で、系統的破綻と有限標本を分ける必要があった。

# 2026-09-22 WP05-bR 境界条件の独立再検証

## 実行条件

不通過した一点$\delta=0.02,r=32,q=8$に限定し、新しいmaster seedで独立32 trajectoryを生成した。
選択policy/full basis、cosine/sine軸、$q=1,2,4,8$を同じcompiler条件で直接transpileした。

## 結果と判断

選択policyの$q=8$ RZ誤差は0.516%、全metric最大0.537%、full basis RZ誤差は0.829%だった。
選択policyの$q=4$全metric最大誤差も1.094%で、全checkが5%基準を通過した。初回超過は独立高統計
再検証で再現せず、H4の明示条件ではWP05-bを完了とした。ただし$q>8$、別snapshot、別compiler、
coupling map付き条件への精度移送は主張しない。

## WP05-b/R成果物

- `artifacts/research_direction_full_scope_extension/2026-09-22/wp05b_q8_delta_0p01_full_scope_extension_v1.json`
- content fingerprint `363ac90a...bfda`
- `artifacts/research_direction_full_scope_replication/2026-09-22/wp05br_r32_32trajectory_replication_v1.json`
- content fingerprint `7bfeddac...472`

# 2026-09-22 WP01-D/C07候補別再最適化

## 実行条件

同じH4、1.0 Å、STO-3G、8 qubit、rank 12、CA/10、$\beta_{\mathrm{RPE}}=0.40$ rad、
$\alpha_{\mathrm{tot}}=0.05$を用いた。候補は$L_D=3$の`support_run_le_1`と$L_D=12$の
決定論endpoint、$\delta=0.01,0.02$である。WP05-bRまでの$q=1,2$ affine full-wrapper costと
$q=4,8$ holdoutを用い、compiled RZを目的にcost-weighted $\alpha$、PF/RTE/statistical budget、
整数shot数を候補ごとに再最適化した。coarse/fine gridに加え、上位20解のPF/RTE制約境界を
反復的に締めた。

## 結果と判断

両候補とも$\delta=0.02$を選んだ。$L_D=3$は13,538 shot、点推定
$1.4557921\times10^{12}$、$L_D=12$は11,162 shot、$1.6911234\times10^{12}$で、
$L_D=3$が13.916%低かった。最後の3 roundは総costの90.94%と83.03%を占めた。

5% local model区間は$L_D=3$が$[1.30654,1.60504]\times10^{12}$、$L_D=12$が
$[1.60657,1.77568]\times10^{12}$で、下側候補は$L_D=3$となった。ただし分離幅は
$1.523\times10^9$、$L_D=12$点推定の約0.090%に過ぎず、対称model discrepancyの分離閾値
5.0484%に対する余裕は0.0484 percentage pointだった。25%移送区間はそれぞれ
$[1.01538,1.89620]\times10^{12}$、$[1.26834,2.11390]\times10^{12}$で重なる。

従って、local model条件付きでは$L_D=3$を候補とするが、頑健な方向判断は
`undetermined_under_transfer_sensitivity`とした。これは状態準備、実backend、noise、
別snapshot/系サイズまたは最終的な科学的優位性を含む総cost評価ではない。次はM08/G08として、
支配的な後半3 round付近の$q>8$ proxyを直接較正・holdoutし、僅かなlocal marginが維持されるか
調べる。

## WP01-D/C07成果物

- `artifacts/research_direction_decision_cost/2026-09-22/wp01d_c07_full_scope_optimization_compute_v2.json`
- content fingerprint `709142f7...474`
- `artifacts/research_direction_decision_cost/2026-09-22/wp01d_c07_conditional_interval_synthesis_v1.json`
- content fingerprint `7d85b472...250f`

いずれもlocal dirty-worktree evidenceであり、immutable CIまたは外部再現ではない。
schema-v1 computeはgrid下端依存を検出した予備診断であり、判断入力には使わない。
変更後のlocal全suiteは`530 passed, 4 warnings`で、warningは既存grouped-UWC test由来である。

# 2026-09-22 G08 round dominance

WP01-D/C07 schema-v2 computeと判断統合artifactを新規回路計算なしで再集計した。最後3 roundは
$L_D=3$のcompiled RZ cost 90.94%、較正不確かさ87.70%、$L_D=12$のcost 83.03%を占めた。
$L_D=3$の最大cost/PF-riskはround 17だが、最大finite-RTE riskはround 7であり一致しない。

M08は$\delta=0.02,r=32,K=2$の未使用$q=16,32$だけを各8 fresh trajectoryで直接holdoutする。
G08 artifact fingerprintは`e696ced2...d064`で、専用testを含む部分実行は`3 passed`だった。
これはlocal dirty-worktree evidenceで、最終総costまたは頑健な優位性判定ではない。

# 2026-09-22 M08 holdoutと再集計

G08で固定したH4、$L_D=3$、$\delta=0.02,r=32,K=2,q=16,32$について、各$q$ 8 fresh
trajectoryのselected `support_run_le_1`／full-basis complete Hadamard wrapperを直接transpileした。
selected RZ誤差2.466%、全metric 2.569%、full-basis RZ 3.286%、direct RZ relative SE 1.353%で、
5%、5.0484%分離限界、2%精度診断を通過した。$q=64$ follow-upは事前規則上不要と判定した。

WP01-D/C07の点推定と較正half-widthを保ち、2.466%と観測RZ最大3.286%を両候補共通の対称幅として
再集計した。両scenarioで$L_D=3$区間は$L_D=12$区間より低く分離したが、25%移送区間は重なる。
M08は$q\leq32$だけの直接証拠であり、$q_{\max}=131072$や$L_D=12$を直接測定していないため、
頑健判定は`undetermined_under_transfer_sensitivity`を維持する。次は追加のlocal $q$精密化ではなく、
主張範囲の見直しまたは外部条件での移送検証とする。

M08 artifact fingerprintは`e010a63b...af8`、再集計artifact fingerprintは`6aa69bc7...b3e`。
いずれもlocal dirty-worktree evidenceで、最終総costまたは科学的優位性の判定ではない。
変更後のlocal全suiteは`535 passed, 4 warnings`で、warningは既存grouped-UWC test由来である。
