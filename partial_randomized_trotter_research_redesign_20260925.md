# Partially-Randomized-Trotter：研究テーマの再設計と着地点

**研究方向を一つに固定するためではなく、異なる成果の形を比較し、必要な実験を目的から選び直すための設計書。**

作成日：2026-09-25<br>
研究証拠の基準：`HIROMU1015/Partially-Randomized-Trotter`、commit `898da8487aca30a176ce5931d34a923c28c34ac9`。その後のA0等の実施は本書では仮定しない。<br>
位置付け：既存結果・一次文献の読解と研究設計。新しい数値実験、回路改善、定理の新規性確定、投稿・採択の保証を行った文書ではない。

---

## 目次

- [0. 判断の要約](#s0)
- [1. 研究の方向と着地点を分ける](#s1)
- [2. 既存結果から何が言えるか](#s2)
- [3. 文献照合による新規性の再評価](#s3)
- [4. 八つの候補の比較](#s4)
- [5. R1：回路列の基底・部分空間を共同設計する](#r1)
- [6. R2：長いランダム回路の期待コストを構成的に予測する](#r2)
- [7. R3：部分ランダム化が有効になる領域を予測する](#r3)
- [8. R4：固有値誤差とスペクトル重みを合わせたPFの使用条件](#r4)
- [9. R5：ランダム化負担を含むエネルギー推定用PFの設計](#r5)
- [10. R6：DF表現とサンプリングの資源目的設計](#r6)
- [11. R7：構造変化に伴うエネルギー差へ研究対象を移す](#r7)
- [12. R8：深さ制約の下で時間発展と位相推定を選び直す](#r8)
- [13. 独立した研究としてまとめる四案](#s13)
- [14. 今から行う判別と、その後の分岐](#s14)
- [15. 完了条件・中止条件・成果の表現](#s15)
- [16. 既存コードと先の追加検証をどう扱うか](#s16)
- [17. 出典と確認範囲](#refs)

<a id="s0"></a>
## 0. 判断の要約

**現行プロジェクトをそのまま完成させることを目的にする必要はない。現在の検証は、研究テーマを選び直すための基盤として使える。ただし、実装上の改善を、そのまま新しい研究上の貢献と見なしてはいけない。**

特に今回の文献照合では、次を区別する必要があると分かった。

| 一見、新しい成果に見えるもの | 既知の部分 | 本研究でさらに必要な差分 |
|---|---|---|
| supportに関係する軌道だけを回す | 部分基底変換と低ランク実装は既知 | 複数eventを通した補完自由度・基底遷移・分割を共同最適化する |
| 回路コストとshot数を同時に最適化する | 一般的な資源目的のimportance samplingやPR資源モデルは既知 | DF回路列固有の構造、実行可能な推定器、選択を変える条件を示す |
| 固有値誤差を直接小さくするPF | spectral analysis、固有値用PF、分割最適化は既知 | 有効な信号重み、ランダム化負担、差分エネルギー等の未解決部分へ絞る |
| 高次PF・ランダム化・分解を組み合わせる | SPRINTなどの統合設計が既にある | 対象タスクに固有の設計条件と、強い既存法に対する差分が必要 |
| エネルギー差で誤差相殺を使う | 固定核配置の低励起状態間の相殺は既報 | 異なる核配置間での相殺を予測・保持・設計できるかを問う |

これらは研究を断念する理由ではなく、**何を新しい主張として残すかを絞る理由**である。[W1][W1]–[W9][W9]

現時点で比較する価値が高い主題は、次の三系統である。

**系統A：回路・合成。** R1を核にし、R2を必要な範囲で組み合わせる。現在の実装との距離は最も短い。ただし既知の部分基底変換を入れた強いbaselineに対する差が必要。

**系統B：誤差・信号。** R4を核にする。エネルギーbiasだけを最小にする設計が、実際の位相推定に使えるのかを調べる。RTEは必須ではない。単なる既知の重み下界の再導出では不十分。

**系統C：化学的な差分量。** R7を核にする。絶対基底エネルギーから、同じ電子状態の異なる構造間のエネルギー差へ移る。大きな方向転換だが、現在のPF誤差計算を生かせる。既知の相殺を別系で観測するだけでなく、予測・設計へ進める必要がある。

R5は、係数探索・解析を主にしたい場合の別の有力候補である。R3、R6、R8も残せるが、広い比較や新しい最適化基盤が必要になりやすく、中心の科学的問いをさらに狭めてから着手する方がよい。

これは正式な研究方針の変更ではない。T4/T7主軸という既存の実行判断と、以下の新しいテーマ選定案は分けて扱う。[R1][R1]、[R2][R2]

<a id="s1"></a>
## 1. 研究の方向と着地点を分ける

### 1.1 「方向」は動詞、「着地点」は主張と成果物

「コストを調べる」「PFを最適化する」「別分子へ広げる」は作業の方向であって、完了条件ではない。

本書では着地点を、次の四つの組として定義する。

1. **主張**：誰に、どの条件で、何が新しく分かったと言うか。
2. **方法**：その主張を実現・予測・検証する式、アルゴリズム、指標。
3. **証拠**：対照、未使用条件、失敗例、不確かさ、再現可能な計算。
4. **終了線**：どの範囲まで示せば、一つの研究として閉じるか。

「全量子化学系で最良」「全compilerで改善」「物理量子優位性まで証明」を共通の終了条件にはしない。特定の回路モデル・問題族で閉じた方法論や、条件付きだが意思決定を改善する数値研究も、候補になる。

### 1.2 三段階の着地点

各テーマに以下を用意する。

- **最小の完結点**：弱い途中結果ではなく、一つの限定された問いへ答える成果。論文採択を保証するものではない。
- **目標の着地点**：予測または設計が未使用条件で働き、他の研究者が使える形。
- **発展先**：大系、厳密保証、別タスク、実機・FT資源等。最初の成果に全部含めない。

### 1.3 実行可能性の比較方法

締切、担当人数、利用可能な計算時間、目標投稿先は新たに仮定しない。代わりに、追加作業を「数式・証明」「古典状態計算」「回路合成」「統計・推定器」「大規模評価」に分け、現在の基盤からの距離を比較する。

正確な基底状態を古典計算で用いるbenchmarkと、古典的に解けない問題にも使える設計手順は異なる。各候補で、どちらを着地点にするか明記する。前者にも研究上の役割はあるが、後者を無条件に主張しない。

<a id="s2"></a>
## 2. 既存結果から何が言えるか

### 2.1 今ある証拠

基準commitのM06-Fは、H4・1.0 Å・STO-3G・DF rank 12・同一snapshot・Qiskit 1.3.0 opt2・状態準備なしのcontrolled Hadamard回路を評価している。候補は主にL_D=3,12、δ=0.01,0.02、K=2、r≤32である。[R2][R2]、[R3][R3]

| 内容 | 確認できること | まだ言えないこと |
|---|---|---|
| 51/51 task、precision/holdout基準通過 | 指定した局所compute campaignの完了 | 全条件・全回路長の検証完了 |
| PR側1.263314e12、決定論側1.327822e12 RZ | 限定候補の点差は4.858% | 一般的優位性、同等性、最適分割の確定 |
| 全不確かさ区間が重複 | 現在の幅では勝敗が頑健でない | 部分ランダム化が無価値・必ず不利 |
| β・α配分が両者へ効く | 比較相手にも最適化を与える必要 | 改善をランダム化固有の効果と解釈すること |
| 単発support限定は安いが長い同basis列で逆転 | event単体と列全体の最適化が競合 | この競合に関する一般定理や新規性の確定 |
| 固定policyが短列holdoutで改善 | 実装候補としての局所的な有効性 | 未使用分子・別compilerへの一般化 |
| WP03で係数選択が不変 | 固定budget・shortlistでは小差が選択へ効かなかった | PF次数や分割・刻みの最適化が重要でないこと |

数値の根拠は[R2][R2]–[R6][R6]。本書では保存結果を再実行したとは扱わない。

### 2.2 新しい問いの種

既存結果から生じた問いは、少なくとも次の四つに分かれる。

**回路の問い**：どの部分空間を回し、どこまで基底を共有すれば、列全体を安く実装できるか。

**予測の問い**：全部compileせず、どの情報まであれば良い候補を選べるか。

**誤差の問い**：固有値biasが小さいという理由だけで、そのPFは位相推定に使いやすいと言えるか。

**対象量の問い**：本当に絶対エネルギーの精度を追うべきか。目的量を差分へ変えた方が、意味のある設計ができないか。

前二つだけに拘束される必要はない。後二つは既存のPF・state-action・固有位相計算を別の科学的問いに使う方向である。

<a id="s3"></a>
## 3. 文献照合による新規性の再評価

### 3.1 重要な修正：support限定変換そのものは既知

Mottaらの低ランク分解論文は、必要な軌道部分空間だけの基底変換と、隣接する基底変換の合成を扱う。元のPR論文も、回転された少数軌道のPauli作用を低コストで実装する構成を使う。[W1][W1]、[W2][W2]

従って、現在の「full basisに比べて単発Z/ZZが大きく安い」は、有用な実装検証ではあるが、それだけで新しい回路原理を発見したとは言えない。

R1を選ぶ場合の新規性候補は、**既知の部分基底変換を出発点として、使われない軌道の補完自由度を列の前後関係で選び、遷移を共同最適化すること**である。単に同じbasisをまとめることも既知最適化に含め、対照を弱くしない。

### 3.2 エネルギー専用PF・分割最適化も既知

Yi–CrossonはPFのスペクトル誤差を分析し、Hejaziらは固有値の精度を意識したPFを構成している。分割選択へ摂動的固有値誤差を使う研究もある。[W4–W6]

従って「operator normでなくCを小さくする」は研究分野として重要でも、今回初めての着想ではない。R4なら計算可能な重み診断と位相推定の使用条件、R5ならランダム化の時間増幅を含む設計、R7なら異なる構造間の差分誤差を対象にする必要がある。

### 3.3 統合設計・資源目的samplingとの重複

SPRINTはnear-integrability、randomization、processing、symmetry protection、factorizationと回路実装を統合している。一般的なcost-aware importance samplingには、任意の回路costに対する最適分布の結果がある。[W7][W7]、[W8][W8]

「複数の工夫を合わせた」「非加法costをsamplingへ入れた」というだけでは、差分を特定できない。特にW8はcostを単純なevent和へ限定していない。長い列でその分布を効率よく生成する方法、weightの裾を含む有限標本の扱い等まで問題を絞る必要がある。

### 3.4 明示的に残されている問い

SPRINTの対象例はスペクトルpeakの位置誤差を主に評価し、スペクトル重みの変化は将来課題としている。これはR4へつながる。ただしPF固有ベクトル誤差の一般論自体は既知である。[W4][W4]、[W5][W5]、[W7][W7]

2026年のnanographene研究は、固定核配置での低励起状態間のTrotter誤差相殺を報告し、同じ電子状態の異なる核配置間のエネルギー差を別の未検討対象として挙げる。これはR7を検討する具体的な動機になる。[W9]

「一論文のfuture workにある」ことは、文献全体で未研究である証明ではない。以下の新規性は**確認した文献との差分候補**であり、テーマを採用する前に、その差分そのものを中心に引用文献・関連論文をさらに絞って確認する。

<a id="s4"></a>
## 4. 八つの候補の比較

本書のR1–R8は、新規の研究テーマ候補IDである。既存カタログのR01–R08、T1–T7、WP番号とは別物であり、検証が増えたという意味ではない。

| 候補 | 問いの中心 | 主な着地点 | 今のコードとの距離 | 最大のリスク |
|---|---|---|---|---|
| R1 | 基底・部分空間を列全体でどう選ぶか | 意味論を保つ合成法と、未使用列での改善 | 近い | 既知の部分変換・融合だけで差が消える |
| R2 | 長いランダム回路costを全部作らず予測できるか | 明示compilerの期待count式、誤差付き選択手順 | 近い～中 | 局所モデルで大域compileを保証したことにしてしまう |
| R3 | PRが有効な条件は何か | 公平なbaselineに対する成立領域・説明指標 | 近いが評価は広い | 列挙表のみに終わり、予測性がない |
| R4 | biasが小さくても信号は使えるか | 固有値・重み・半径を合わせた使用条件 | 近い～中 | 重みの評価に全対角化が必要で終わる |
| R5 | 固有値精度とランダム時間負担を両立できるか | 専用PF familyまたは限定classの選択則 | 中 | 既存PF/SPRINTに支配される、内側誤差を無視する |
| R6 | DF表現・samplingを何のために最適化するか | 総資源と整合する目的関数・実行可能な設計 | 中～遠 | 既存factorization/ISとの差分がない |
| R7 | 構造間エネルギー差の誤差を予測・設計できるか | 差分用の分解/PF選択則と適用条件 | 中、物理タスクは大きく転換 | 相殺が不安定、既知現象を再観測するだけ |
| R8 | 1回路深さに制限があると何を選ぶべきか | 深さ・総cost・精度のPareto関係 | 中～遠 | 既存低深さ位相推定の比較をなぞるだけ |

共通する注意：基盤の再利用率が高いことは費用面の利点だが、新規性や意義の根拠ではない。逆に、大きな方向転換でも、明確な問いと小規模な判別があれば検討可能である。

<a id="r1"></a>
## 5. R1：回路列の基底・部分空間を共同設計する

### 5.1 Research question

> 各eventに必要な部分空間を保ったまま、使われない軌道の自由度を列の前後に合わせて選ぶことで、既知の部分基底変換と隣接融合を適用した回路より安く実装できるか。

RTE分布自体は変えず、サンプルされた**同じ順序・同じ演算**の実装だけを変える研究として切り出す。これならPF誤差、finite-RTE打切り、RPE推定器の再設計を同時に完成させる必要がない。

### 5.2 出発点となる数式

Gaussian基底変換のFock空間表現をB_j、対角作用をD_jとして、eventを

\[
V_j=B_jD_jB_j^\dagger
\]

と書く。D_jと可換なGaussian変換C_jであれば、

\[
(B_jC_j)D_j(B_jC_j)^\dagger=V_j
\]

であり、同じeventの実装に自由度がある。非作用軌道上の変換はその典型例である。この自由度自体は既知の部分変換の考え方に含まれる。[W1][W1]、[W2][W2]

新しく設計する対象は、C_jを一つずつ独立に選ぶのではなく、**隣接する(B_jC_j)^\dagger(B_{j-1}C_{j-1})の実装負担まで含めて選ぶこと**である。

候補frameを有限集合s_jから選び、明示的に実装する回路のcostが

\[
C(\boldsymbol{s})=c_{\mathrm{start}}(s_1)
+\sum_j c_j(s_j)
+\sum_{j=2}^{L}t_j(s_{j-1},s_j)
+c_{\mathrm{end}}(s_L)
\]

へ分解できるように構成するなら、最短路・動的計画法で**その候補集合とcostモデル内の**最小値を求められる。

これは任意のQiskit全体transpile後costに対する最適性ではない。非局所的な最適化が入る場合は、明示回路のcountと最終compilerのmetricを別に評価する。event順序の並べ替えは一般に演算を変えるため、無条件には許さない。

### 5.3 具体的な設計候補

第一候補は「同一basisのrunを、supportの和集合が小さい区間へ分割し、その区間だけ共通の部分基底変換で処理する」方法である。singletonとfull共有の二択を、区間単位の選択へ広げる。

第二候補は、非作用軌道のcompletionを前後のframeに合わせる方法である。個々のcompletionを最も疎にするより、相対変換を安くする方が列全体では有利かを問う。

両方を最初から同時に最適化しない。まず有限候補の区間分割を対象とし、そこに差が残る場合だけcompletionの連続最適化へ進む。

### 5.4 科学的な意味

「単発gateを短くする」ではなく、**同じHamiltonian simulation event列に対して、局所的な最適化が大域的な損失を生む条件を特定し、その損失を避ける合成法を与える**ことに意味がある。

RTEは応用例であってもよい。別のDF回転列や測定basis列への移送は発展先となるが、対象演算の同値性とcost定義を改めて確認する。

### 5.5 必須baseline

full basisを毎回復元する素朴な実装だけとの比較では足りない。少なくとも、既知の部分基底変換、同basis共有、異なるbasis間の相対変換融合を使った実装を対照にする。現行`support_run_le_1`も独立baselineにする。[W1][W1]、[W2][W2]、[R4][R4]

短列では有限候補を全探索して、提案法の近似損失を測れる。全探索最良を長列の実用法と呼ばず、少数の検証用oracleとして使う。

### 5.6 着地点

**最小の完結点**：対象を特定のDF event列と回路モデルに限定し、正しい合成法、同値性の説明、既知最適化済みbaselineに残る改善または最適な使い分けを示す。数個の都合のよい列だけでなく、未使用列と悪化例を含む。

**目標の着地点**：run長だけでなくsupport和集合・重なり・frame遷移からcostを見積もる選択法を作り、調整に使わないsnapshot／系サイズでも改善を予測する。回路の古典生成時間も計測し、量子cost減少が極端な古典前処理を要求しないことを示す。

**発展先**：有限候補classの厳密最適性、広いGaussian演算群、routing込み、Clifford+T合成込み。これらは最初の完了要件ではない。

**主張してはいけないこと**：support限定のアイデアを新発見とすること、状態準備回路での等価性を任意入力のcontrolled演算へ流用すること、短列のRZ改善を全RPE総cost改善と呼ぶこと。

### 5.7 欲しい図と最初の判別

図は、(i) run長×support和集合に対する各構成のcost、(ii) 最適化済みbaselineと提案法の対応付き散布図、(iii) 未使用instanceの改善・悪化分布、(iv) countとdepthのPareto関係、の四枚で構成できる。

最初の判別では、既知の部分変換と相対basis融合を入れたbaselineを確定し、同basis・別support・異basisの短列へ適用する。その上で区間分割が差を生むかを見る。**既知法だけで差が消えたら、R1の「新合成法」主張は保留する。** 実装改善は残してよいが、それを論文の独立した新規性にはしない。

### 5.8 実行可能性

再利用：DF event builder、support completion、controlled同値性、対応付きtrajectory測定。新規：強いbaseline、区間・frame選択、未使用構造の試験。量子shotやfull RPEは最初は不要。現在からの距離は短いが、**新規性のゲートが最初にある**。

<a id="r2"></a>
## 6. R2：長いランダム回路の期待コストを構成的に予測する

### 6.1 Research question

> 長い回路を直接生成・transpileせずに、実行可能な回路の期待countと候補選択の不確かさを、局所構造からどこまで評価できるか。

単にq=1,2で直線fitし、巨大qへ外挿する研究ではない。**なぜそのcost則が成立するかを説明できる範囲のcompilerを明示的に作る**ことを中心にする。

### 6.2 二つの層を分ける

第一層は、仕様を限定したstreaming compilerである。保持するframe、直前のbasis、未確定run等を状態sとして、event eが入るたびに状態とemitするgate数を更新する。有限状態化が可能なclassなら、確率遷移Pとrewardから

\[
\mathbb E[C_L]=\sum_{j=0}^{L-1}\pi_0P^j\bar c
+\text{開始・終了処理の期待cost}
\]

を計算できる。この式は有限状態reward modelの基本であり、それ自体を新しい定理とはしない。研究上の問題は、実際のDF列に必要な記憶を何で表し、状態数を抑えながらどこまで有効な合成を表せるかである。

第二層は、この明示回路に対してさらに一般compilerをかけたときの差である。こちらは実測・holdout・domain外検出を用いる。第一層の正確な式を、そのまま一般compilerの正確な式と主張しない。

countに対する式が得られても、depthは依存DAGの最大経路を含むため自動的に同じ扱いにはならない。最小の研究は期待RZ/CX countで閉じてよい。

### 6.3 意思決定への接続

候補aのcostを区間[L_a,U_a]で評価できるとする。区間が全候補を同時に覆うことを正当化できる場合、

\[
U_a\leq\min_{b\ne a}L_b
\]

ならaの選択を認証できる。また、

\[
U_a-\min_b L_b\leq\eta
\]

なら、絶対costでη以内の選択を認証できる。

これも基本的な区間推論であり、独立した新規性ではない。新規性候補は、**この判定に必要な追加測定・局所構造評価だけを選び、全候補の精密化より少ない古典計算で同じ意思決定へ到達する方法**である。既存のbest-arm／multi-fidelity最適化との差分確認も必要になる。

経験的holdoutの最大残差を、無条件の同時信頼区間へ置き換えない。sampling、モデル仮定、compiler条件、外挿を別欄で残す。

### 6.4 着地点

**最小の完結点**：限定compilerとevent分布について、期待countの正確な式または明示的な近似誤差評価を与え、直接生成した短～中列と一致させる。巨大回路を生成しなくても、少なくとも一つの実行可能な構成の資源を計算できる。

**目標の着地点**：異なる分割・RTE条件の候補選択へ接続し、全compile対照と同等の選択を、より少ない古典計算で行える。予測誤差だけでなく選択損失と計算時間を報告する。

**発展先**：depth、routing、複数compiler、信頼性を伴う適応測定。この段階でも全量子回路の最小gate数を保証するものではない。

### 6.5 この研究が弱くなる場合

一般compilerに対する無根拠な長q外挿を、単に多数のtestとartifactで包むだけでは不足する。推定式が普通の線形回帰と同程度で、選択も計算負荷も変わらないなら、R2は独立主題にせずR1/R3の補助法にする。

### 6.6 図・判別・実行可能性

図：列長に対する実costと構成式、残差とsamplingの分解、候補の選択損失対古典計算量、適用domainを外れた反例。

最初の判別：R1の候補のうち小さい記憶で表現できる合成policyを一つ選び、短列の全列挙でreward modelを確かめる。ここで状態数が急増するなら、classを絞るか実測modelへ役割を限定する。

現在の境界補正・cost provider・不確かさ台帳を再利用できる。新しい価値を出すためには、回帰の精度だけでなく**構成可能性または選択計算の削減**が必要である。

<a id="r3"></a>
## 7. R3：部分ランダム化が有効になる領域を予測する

### 7.1 Research question

> 精度、tailの係数分布、PF誤差、基底変換負担のどの組合せで、部分ランダム化が最適化済み決定論PFより有利／不利になるか。

現行の研究目的に最も近い。ただし「H4の点順位を確定する」から、「条件を変えたときの選択を説明・予測する」へ着地点を変える。[R2][R2]、[R3][R3]

### 7.2 出発点のモデル

固定分割・固定pの概念モデルとして、

\[
G_{\mathrm{PR}}\approx a_D\epsilon^{-1-1/p}
+b_R\lambda_R^2\epsilon^{-2},\qquad
G_{\mathrm{det}}\approx a_F\epsilon^{-1-1/p}
\]

を置く。これはPRの既存スケーリングを簡略化したもので、compiled costの厳密式ではない。[W1]

p>1かつa_F>a_Dなら、このモデルのPR有利条件は

\[
\epsilon>
\left(\frac{b_R\lambda_R^2}{a_F-a_D}\right)^{p/(p-1)}
\]

となる。固定tailを残したまま精度を厳しくすれば、常にPRが有利になるわけではない。分割も精度に応じて変える場合は、この固定分割の式だけでは結論できない。

ここでの式変形は研究成果ではない。研究として必要なのは、a_D、b_Rを実装構造と結び付け、整数roundやbasis共有がこの予測をどこで変えるかを、未使用条件で示すことである。

### 7.3 何を特徴量にするか

λ_Rだけでなく、決定論1-step cost、符号付きPF係数、RTE eventのcost分布、basisの衝突確率、run長、必要short-step数、信号半径を記録する。`exact_rte_lambda_r`は選択されたLCU表現の係数L1であり、一般に演算子normそのものではない。

たくさんの特徴を入れた回帰に直ちに進まず、「tailが重い」「basis切替が多い」「PF制約が強い」の三つの機構を最初に切り分ける。

### 7.4 公平な対照

同じHamiltonian・精度・成功確率・cost scopeで、それぞれのδ、配分、許される公式を最適化する。決定論二次との比較は導入効果を示す。広く決定論PFより有利と主張するには、有力な四次等の代表baselineも必要になる。

異なるDF rankや表現を比較する場合は、近似Hamiltonianの誤差も同じ精度予算へ入れる。回転合成を含まないRZ結果は、そのgate modelの資源結果として閉じてよい。FTのT/Toffoli優位性へ自動拡張しない。

### 7.5 着地点

**最小の完結点**：固定した問題族・精度範囲・compiler内で、PRと決定論を選び分ける表と、その境界を説明するモデルを作る。負ける場合も含め、強い対照と不確かさを報告する。

**目標の着地点**：少数の古典計算で得られる特徴量から、未使用instanceの候補集合を選べる。予測が曖昧なら「要詳細評価」と返し、無理に勝敗を出さない。

**発展先**：一般的な不利領域の解析条件、深さ制約込みPareto関係、より大きな分子。H4だけの結果を大系へ外挿するのは発展先の達成ではない。

**否定的結果として閉じる条件**：単にerror barが重なることでは不足。有限だが明示した候補域で、資源低下が難しい理由、あるいはbaselineが低い条件を再現可能に示す。全手法に対するno-go theoremと称さない。

### 7.6 判別と負荷

この方向を残す場合にだけ、先のレビューのδ/r境界、他prefix、強いbaselineが直接必要になる。最初は上端に当たる変数を一段広げ、候補差を左右する要因を特定する。粗いproxyで不利というだけで候補を厳密棄却しない。

コードとの距離は近いが、研究としての一般性には複数条件の評価が必要で、合計計算量は大きくなりやすい。単一H4で精密化し続けるより、対照の強さと境界の予測性を優先する。

### 7.7 2026-09-26先行研究監査による更新

P-D S1事後再解析後にclosest workを再監査した結果、fragment norm、PF誤差、per-step cost、
randomized remainderを合わせて分割・公式を選ぶ広いR3は、SPRINTを含む既存研究との重複が強く、
独立主題としてはNo-Goとした。

残すのは、ordered DF prefix固有の安価な診断により、高価な詳細評価と同じ選択を認証するか棄却し、
未使用条件でregretと詳細評価数を同時に評価する狭いR3-Sだけである。一般のmulti-fidelity best-arm identificationおよびcertified multi-fidelity optimizationまで追加監査したが、
quantum-specificな保証または方法論差分を固定できなかった。R3-S0は`STOP_R3_NO_METHOD_DELTA`とし、
条件付き最小契約の数値部分は実行しない。詳細は
[R3先行研究監査と条件付き最小研究契約](docs/research/r3_prior_art_and_minimal_contract.md)を参照する。

<a id="r4"></a>
## 8. R4：固有値誤差とスペクトル重みを合わせたPFの使用条件

### 8.1 Research question

> PFの固有値biasが小さいだけでは不十分になるのはいつか。対象状態のスペクトル重みと信号半径まで含めて、位相推定に使用できる時間刻み・公式を、全PF対角化より安く判定できるか。

ここではRTEを外して始められる。finite RTEは、最後に既知の減衰・誤差として接続する応用先にできる。現在のD6対固有位相の一致を0.1%まで追うこととは、別の研究である。

PF固有ベクトル誤差と固有値誤差の区別は既知であり、SPRINTでもpeak位置に加えて重み変化を扱う余地が残る。[W4][W4]、[W5][W5]、[W7][W7]

### 8.2 数学的な出発点

固定したunitary S(δ)の固有状態をφ_j、固有位相をθ_jとし、入力ψの重みをw_j=|⟨φ_j|ψ⟩|²とする。

\[
Z_q=\langle\psi|S(\delta)^q|\psi\rangle
=\sum_j w_j e^{-iq\theta_j}.
\]

対象分枝を0とすると、w_0>1/2のとき三角不等式から

\[
|Z_q|\geq2w_0-1
\]

であり、対象位相に対する円周上の偏差は

\[
|\arg(Z_qe^{iq\theta_0})|
\leq\arcsin\left(\frac{1-w_0}{w_0}\right)
\]

で抑えられる。これは全整数qに対する基本的な幾何学的事実であって、新規定理ではない。w_0≤1/2で下界が使えないことも、その信号が必ず失敗することを意味しない。

問題は、w_0をどう知るかである。孤立した固有値を摂動的に追える範囲で

\[
H_{\mathrm{eff}}=H+\delta^pD+\cdots
\]

で、入力を元のHの対象固有状態ψ₀とした場合、leading orderでは

\[
\Delta E_0\simeq\delta^pD_{00},\qquad
1-w_0\simeq\delta^{2p}
\sum_{j\ne0}\frac{|D_{j0}|^2}{(E_j-E_0)^2}
\]

となる。対角成分の相殺でbiasが小さくても、非対角成分と小gapが大きければ重みは悪化し得る。これも摂動論の出発点である。[W4][W4]、[W5][W5]

### 8.3 新規性を置く場所

候補は、state-actionや少数の低エネルギー状態から、対角biasと重み移送を同時に推定する実用的な診断法を作ることである。全PF対角化を使ってw_0を観測しただけでは、一般論の確認に留まりやすい。

例えば、適切な参照位相zに対する残差ε_res=||(S−z)ψ||と、他の固有位相への距離g>0の正当な下界があれば、対象外重みはε_res²/g²で抑えられる。gはunit circle上の固有値の距離であり、元のHのエネルギーgapをそのまま代入できるとは限らない。phase wrappingと参照分枝の特定が別途必要である。

実用的にgを厳密に求められない場合は、保証付き版と経験的診断版を分ける。真のPF固有状態を必要とする計算を、古典的に解けない問題にも安く使える診断法と呼ばない。

### 8.4 finite RTEへの接続は後段

正規化補正後のfinite-RTE信号誤差がε_Z以下なら、条件が満たされる範囲で観測半径の保守下界を

\[
A_{\mathrm{att}}(2w_0-1-\epsilon_Z)>0
\]

と置ける。PFの固有位相bias、非支配分枝の汚染、RTE近似、shot誤差を別々に管理する。

これにより、単にCδ^pが小さいから大きなδを採用するのではなく、使用可能な信号を残す範囲でδを選べる。実際に候補選択やcostが改善するかは検証対象である。

### 8.5 着地点

**最小の完結点**：明示したDF/PF classで、biasだけの基準が不適切になる条件を示し、重みを含む診断がその失敗を事前に捉える。反例は単なる人為的2×2行列だけでなく、対象問題族の一例と結び付ける。

**目標の着地点**：全対角化を要しない係数・残差推定から、許容δやPFの採否を予測し、未使用条件のRPE信号・分枝復元で検証する。予測を使った設計が、energy-only基準の過剰な保守性か不適格選択の少なくとも一方を改善する。

**発展先**：複数peakの同時推定、近接・縮退部分空間、準備状態の不完全性、低エネルギー大系への移送。

### 8.6 最初の判別と中止条件

小規模系で、gapやδを変え、同程度のenergy biasなのに対象重み・信号が異なる候補が存在するかを見る。既存artifactの再解析を先に行い、PF全体の新しい大規模実装は不要。

対象とする広さの範囲でweight効果が常にcost選択より十分小さく、既知の一般下界で十分なら、独立主題にはせずR3/R5/R7の安全診断として残す。逆にenergy-only最適化と選択が食い違い、その差を少数の情報で説明できれば、研究の中心にする根拠ができる。

<a id="r5"></a>
## 9. R5：ランダム化負担を含むエネルギー推定用PFの設計

### 9.1 Research question

> 固有値誤差を抑える利益と、ランダムtailを繰り返す絶対時間・回路負担を同時に考えたとき、どのPF構成が適するか。決定論用の良い公式をそのまま使うより良い設計が存在するか。

これは「2次を8次に替えてみる」より狭く、かつ具体的な設計問題である。高次partial-RTEの全面実装から始めず、少数blockの係数条件と資源proxyから始める。

### 9.2 既知研究との差分

固有値誤差のためのPFは既にあり、高次・near-integrable・randomizationの統合も既に研究されている。[W5][W5]、[W7][W7] 従って、形式次数だけ高めることは差分にならない。

差分候補は、**RTEのnormalizationを増やすtail係数の絶対値と、各stageの回路costを明示的に目的関数へ入れ、固有値精度の条件を満たす係数を選ぶこと**である。既存のPR論文の高次議論、negative-time議論との重複も確認する。[W1]

### 9.3 設計用の出発点

tailのstage時間をt_j=b_jδ、そのstageのshort-step数をr_jとする。独立なRTE構成のnormalization上界から、

\[
B_{\mathrm{tot}}\leq
\exp\!\left(\lambda_R^2\sum_j\frac{t_j^2}{r_j}\right)
\]

という設計上の上界を使える。[W1]

連続緩和でR=Σr_jを固定すると、Cauchy–Schwarzにより

\[
\sum_j\frac{t_j^2}{r_j}\geq
\frac{(\sum_j|t_j|)^2}{R}.
\]

1 short stepのcostをc_j>0としてC_R=Σc_jr_jを固定する場合も、

\[
\sum_j\frac{t_j^2}{r_j}\geq
\frac{(\sum_j|t_j|\sqrt{c_j})^2}{C_R}
\]

となる。これは基本不等式からの設計用診断であり、**実測総costの下界や、新しい量子計算量の定理ではない**。Bの上界に基づく負担、有限K、整数r、列内融合、shotへの接続を別途評価する。

このため、同じ固有値精度でも、

\[
\Gamma_R=\sum_j|b_j|
\]

が大きい公式はランダム側の負担を増やし得る。エネルギー誤差係数、deterministic stage cost、Γ_R、finite-RTE biasのPareto候補を探すのが出発点になる。

### 9.4 誤った高次化を避ける

H_Dの内部だけを高次化しても、H_D/H_Rの外側分割が二次なら、全体の形式次数が自動的に高くなるわけではない。二blockの式でe^{-iH_Dt}を厳密扱いした結果を、実際には多数fragmentをPFで実装するH_Dへそのまま移さない。

また、processorだけの相似変換P†UPはUと同じ固有値を持つ。processorだけで固有位相biasを消す主張はできない。processorが入力状態との重なりやtime-series誤差を改善する可能性と、kernel自体の固有値を変える設計を分ける。

### 9.5 着地点

**最小の完結点**：明示した有限stage familyの中で、energy errorとrandom-tail負担の競合を解析し、既存係数を選ぶ／避ける基準を示す。候補集合内の最良性か、支配されない係数familyを特定する。

**目標の着地点**：新しい係数familyまたは選択則を作り、小さい一般行列とDF化学Hamiltonianの未使用例で、精度・signal条件を保ちながら総資源を改善する。大きいΓ_Rで逆効果になる領域も示す。

**発展先**：state-specific学習、二次のままenergy-orderを上げる構成、near-integrable hierarchical PFとの統合。ただし既存SPRINTの再実装を新提案と呼ばない。

**改善が出なかった場合**：有限familyでどの制約が利益を消したかを解析できれば、設計限界の成果にできる。試した数個が負けたことから高次PF全体のno-goを主張しない。

### 9.6 判別と実行可能性

最初はBCH／Magnus係数とstage列から、標準二次、有力四次、固有値用の代表公式のenergy係数とΓ_Rを比較する。理想化した二blockで差があっても、実際のH_D内部誤差を戻すと残るかを次のゲートにする。

追加作業は係数条件・誤差展開・新PF familyの正当化が中心で、論理的な難しさが高い。回路数を増やす前に解析段階で採否を判断しやすい点は利点である。

<a id="r6"></a>
## 10. R6：DF表現とサンプリングの資源目的設計

### 10.1 Research question

> λやDF rankを単独で最小にするのではなく、基底切替、PF誤差、randomized estimatorの分散まで含めて表現・抽出法を設計すると、何が変わるか。

この候補は広いので、採用する場合は以下の二つのうち片方だけを核にする。

**R6-a：表現を変える。** 既存DF familyの範囲で、rank、orbital gauge、fragment表現の自由度を、タスク資源に合わせて選ぶ。

**R6-b：表現を固定して抽出法を変える。** 長いevent列に対して、評価可能なsampling分布とweightを構成する。

両方を同時に完成させる計画にはしない。

### 10.2 表現側の既知研究と差分

圧縮・正則化DF、symmetryを利用する圧縮、orbital最適化、SPRINT/GRADE等は既存である。単にDF rankや1-normが下がることを発見したとは言えない。[W7][W7]、[W10][W10]、[W11][W11]

候補目的は、例えば

\[
\min_{\xi,L_D,\delta,\mathrm{schedule}}
G_{\mathrm{task}}(\xi,L_D,\delta,\mathrm{schedule})
\quad\text{subject to}\quad
\epsilon_{\mathrm{representation}}+\epsilon_{\mathrm{simulation}}
+\epsilon_{\mathrm{estimation}}\leq\epsilon_{\mathrm{target}}
\]

である。ξは一つの明示したDF family内のパラメータとする。

新規性候補は、λの小さい表現が最適でない原因をbasisの出現・切替構造で説明し、その構造を含む代理目的が未使用条件の総costをよりよく予測すること。変更した表現が異なる近似Hamiltonianを表す場合は、表現誤差を除外して安くなったように見せない。

### 10.3 sampling側で、既に分かっていること

元のevent列分布p(ω)、正のcost c(ω)>0、別分布Q(ω)を考える。w=p/Qが評価でき、Qがpのsupportを覆うなら、重み付き推定は元の期待値を保つ。

資源目的のworst-case second-momentモデルでは、

\[
\mathcal N(Q)=\mathbb E_Q[c]\,\mathbb E_Q[w^2]
\]

に対して、Q*∝p/√cが最適であり、最小値は(E_p√c)²である。これは既存の結果であり、cがeventの単純和に限られるわけではない。[W8]

従って「非加法costを入れたimportance sampling」は、それだけでは新しい拡張ではない。先に、既存trajectoryのcost分布から

\[
\frac{(\mathbb E_p\sqrt c)^2}{\mathbb E_p c}
\]

を診断する。この値が1に近いなら、この固定分布・目的関数での理想IS利益自体が小さい。精密なsamplerを新規開発する前に分かる。

### 10.4 sampling側に残る具体的な候補

候補は、長い列のc(ω)を事前に全compileせず、局所frame modelから評価可能な近似Qを作ること。そのQの正規化・生成cost・weightの裾を扱い、RPEの有限shot保証へ接続する。

平均unitaryを使うHadamard信号と平均量子channelは異なる。channelとして良い近似という理由だけで、coherent信号が正しいと主張しない。weight付きの結果には、元の±1出力用Hoeffding式をそのまま使用しない。元のfinite-RTEにあるbiasも、importance samplingだけでは消えない。[W8][W8]、[W12][W12]

### 10.5 着地点

**最小の完結点（表現）**：一つのDF familyに限定し、λ最小とtask cost最小が違う条件を特定し、簡単な構造目的で選択を改善する。

**目標の着地点（表現）**：表現誤差・PF誤差・実装costを同じtaskで評価し、未使用条件へ転用できる選択則を作る。

**最小の完結点（sampling）**：理想分布の再導出ではなく、実際に生成・評価できる列samplerと有限標本の解析を与え、古典前処理も含めて資源利益を示す。

**発展先**：R1の合成自由度、R3のsplit最適化、別のunbiased simulationとの統合。

### 10.6 判別と実行可能性

既存DF数例またはtrajectoryの再集計から始められるが、その先は非凸表現最適化か確率・推定器の新しい開発になる。既知の最適IS利益が小さい、またはλにbasis情報を足しても選択が変わらない場合は、独立主題へ進まない。

単発のλ削減率を追う研究より、なぜ他のcostが相殺するのかを示す研究の方が、現在の基盤と接続しやすい。

<a id="r7"></a>
## 11. R7：構造変化に伴うエネルギー差へ研究対象を移す

### 11.1 Research question

> 同じ電子状態の異なる核配置間のエネルギー差を求めるとき、PF誤差の相殺を予測し、壊さずに設計できるか。絶対エネルギーを別々に最適化するより、目的の差分量を少ない回路資源で求められるか。

これはPR・finite RTE・RPE総costに固定しない大きな方向転換である。研究対象は「近似時間発展の絶対固有値」から「構造変化に対する相対エネルギー」へ移る。現在のHamiltonian生成・PF構築・固有位相・state-actionを使えるが、RTEを必須にしない。

### 11.2 なぜ具体的な候補か

固定核配置の低励起状態間でTrotter誤差が相殺する現象は既報である。その論文は、同じ電子状態の異なる核配置間の相殺を別の未検討対象として挙げる。[W9]

したがって狙いは「エネルギー差で相殺がある」という一般論ではなく、**構造を動かすとき、どの分解・軌道追跡・PF選択が相殺を保ち、どこで壊すか**である。別の量子化学系への単純適用だけではなく、予測指標または設計則まで必要とする。

### 11.3 出発点となる式

核配置のパラメータをxとする。共通のHilbert空間識別とPF規約を固定し、滑らかな孤立固有値分枝を追える小δの範囲で、対称PFのenergy biasを

\[
\widetilde E(x,\delta)-E(x)
=\delta^p C(x)+O(\delta^{p+2})
\]

と書く。Cは**符号付き**である。異なる構造a,bの差では

\[
\widetilde{\Delta E}-\Delta E
=\delta^p\{C(b)-C(a)\}+O(\delta^{p+2}).
\]

絶対値の小ささだけでなく、Cの構造方向の変化が重要になる。近接構造b=a+hでCが微分可能なら、leading differenceはδ^p h C'(a)である。

ただし、力F=−∂E/∂xにするとleading biasは−δ^p C'(x)であり、**差分幅hを小さくするだけでforceのbiasが消えるわけではない**。エネルギー差の誤差相殺と、微分の数値安定性は分ける。

### 11.4 解析の方向

H_eff=H+δ^pD+…なら、C(x)=⟨ψ(x)|D(x)|ψ(x)⟩である。分枝が滑らかでgapが正なら、

\[
C'(x)=\langle\psi|D'|\psi\rangle
+2\operatorname{Re}\langle\psi'|D|\psi\rangle.
\]

これは、誤差演算子の変化と状態応答の二つに相殺の破れを分ける基本式である。normとgapによる粗い上界を作るだけなら標準的摂動論の範囲であり、それ自体を独自成果にしない。

研究としては、近接する構造に対するCの滑らかさを予測する量、例えば誤差演算子の状態上変化、gap、状態重なり、fragmentの連続性を使う。全点FCIでCを求めてから相殺を確認するだけで終わらず、少数の較正点または近似状態から未使用点の相殺・破れを予測することを目標にする。

### 11.5 具体的な設計対象

第一段階ではPF次数を固定し、(i) 構造ごとに独立に生成した軌道・fragment、(ii) 位相・並び順・近接縮退空間を追跡した連続な表現、を比較する。DF rankの切替や係数thresholdによる不連続を監査する。

第二段階で、絶対誤差|C(a)|や|C(b)|を別々に最小にする規則と、|C(b)−C(a)|を小さくする規則を比較する。後者が未使用構造へ移るかを確認する。各点のbiasを小さくするための独立最適化が、符号や係数の連続性を壊す場合は、明確な機構になる。

構造を超えた同じ軌道ラベルの使用だけでは、物理的な対応を保証しない。active space、原子軌道の重なり、電子状態のラベル、避けられた交差の追跡を定義する。真の交差・縮退では単一分枝の式の適用を止め、対象部分空間の定義を変えるか、その範囲を除外する。

### 11.6 評価量

主要量は差分の絶対誤差、許されるδ、必要step数または同じ推定器での総資源である。補助指標として

\[
\kappa_{\mathrm{cancel}}=
\frac{|C(b)-C(a)|}{|C(a)|+|C(b)|}
\]

を使えるが、分母が小さい点には絶対誤差を併記する。小さいκが単に小さすぎるgeometry差に由来する場合を区別し、目的量の大きさ・必要絶対精度を明示する。

古典的な乱数seedをそろえたことを、独立した量子測定の共分散低減と取り違えない。本研究の中心は決定論的PF biasの相殺である。二つのエネルギー測定の統計誤差は、実際に使う測定方式の下で別に配分する。

現状artifactにはD6と固有位相の符号規約の差が報告されているため、まず全体を「E_eff−E_exact」の共通規約へそろえる。絶対値だけの係数表から物理的相殺を議論しない。[R6]

### 11.7 着地点

**最小の完結点**：一つ以上の明示した構造変化familyで、相殺が生じる条件と壊れる条件を特定し、符号付きleading errorまたはその応答で説明する。既知の固定geometry励起差との差分を明確にする。

**目標の着地点**：連続表現・差分用PF選択によって、絶対エネルギーを独立最適化するbaselineより、同じ差分精度を大きいδまたは少ない資源で達成する。学習・設計に使わないgeometry pairと少なくとも別の条件群で検証する。

**発展先**：反応障壁、相対安定性、force、PES全体、大きいactive space。最初から全てを対象にせず、一つの差分タスクで閉じる。

### 11.8 改善しない結果でも何が残るか

相殺が常に起きるとは仮定しない。例えば軌道・fragment追跡をしても電子状態の変化が支配して相殺が壊れるなら、**大きな刻みを使ってよい領域を判別するルール**が成果になり得る。

一方、少数点で偶然Cが近かった、少数の系で改善しなかった、という結果だけでは不十分である。機構・予測・対照のどれもない場合は、無理に独立研究へ昇格しない。

### 11.9 最初の判別と実行可能性

H4など既存の小系を出発点にしてよいが、現H4の1.0 Å固定実験の続きではなく、構造を動かした別の問題設定である。滑らかな変化と強い状態変化が現れる条件を分け、固定PF・複数δで符号付きbiasと差分biasを計測する。具体的なgeometry値はpilot設計時に選び、ここでは新たな固定条件にしない。

RTE、長いcontrolled回路、全shot最適化なしで最初の判別ができる。追加作業は軌道・fragment追跡、差分対象の定義、未知点への予測である。大きな転換の割に、現在のPF計算基盤との接続は良い。

<a id="r8"></a>
## 12. R8：深さ制約の下で時間発展と位相推定を選び直す

### 12.1 Research question

> 総RZ最小ではなく、1回の回路に許される深さを制約とした場合、どの時間発展法と位相推定法の組合せが有効か。

これは新たに選べる別の問題設定であって、ユーザーの現在のhardware制約を仮定するものではない。深さ上限D_maxを横軸としたfamilyとして研究する。

低深さ位相推定や最大発展時間と総資源の交換関係は既に研究されているため、RPEをQCELS等へ置き換えたというだけでは新規性にならない。[W13]

### 12.2 何を変えるか

現在の2^m round scheduleを唯一の前提にせず、複数時間の統計的推定、既存の低深さ位相推定、deterministic/partial-randomized time evolutionを組み合わせる。

新規性候補は、**時間tではなくcompiled回路深さの分布を制約に使うこと**である。RTEではevent数や構造で深さが変わるため、平均depthだけでは最大実行負担を表せない。

例えば実際にdepth上限を超える回路を失敗とする設計なら、shot数N_mに対して

\[
\sum_m N_m\Pr(D_m>D_{\max})\leq\alpha_{\mathrm{depth}}
\]

のような全実行を通した扱いが必要になる。これはunion-bound型の基本条件であって新規定理ではない。超過eventを単に捨てる場合は、分布変更とbiasを再評価する。

### 12.3 着地点

**最小の完結点**：具体的なdepth制約familyと限られた推定法の集合について、同じエネルギー精度・成功確率での実行可能領域と総資源を比較する。平均depth・depth分位点・総gate数を分ける。

**目標の着地点**：compiled深さ・信号減衰・推定器の誤差を結ぶ選択則を作り、上限制約の変化に応じて使う時間発展法や測定時間を選べるようにする。

**発展先**：具体的なFT architecture、初期状態の準備、実行noise、wall-clock最適化。これらを入れるなら新しい条件として明示する。

### 12.4 注意と実行可能性

roundごとにδや分割を変えると、同じ近似unitaryの累乗ではなくなり得る。従来RPEの分枝関係をそのまま使わず、共通の真のエネルギーに対するround別biasとしてモデル化する必要がある。

新しい位相推定法・推定器の実装と検証が追加されるため、現在からの距離は長い。既存低深さ法との違いが明確でない間は、R3の深さ感度の補足として始め、独立主題に広げない。

<a id="s13"></a>
## 13. 独立した研究としてまとめる四案

八つの方向を全て一論文に入れない。主張の核を一つ置き、他の要素は必要な証拠に限定する。以下は**成果が得られた場合の構成案**であり、既に達成した結果の要約ではない。

### 案A：部分空間自由度を利用したDF回路列の構造適応合成

**核**：R1。R2は、長い列のcost評価に必要な範囲だけ採用する。

**問いを一文で**：「単発eventには不要な軌道の自由度を列全体で選ぶと、どの構造で既知の部分基底変換より安くなるか。」

**完成時に言いたい主張**：「定義したDF event列classに対して、意味論を保つframe/区間選択を構成し、既知最適化済みbaselineとの差を構造特徴で説明・予測できた。」

**想定構成**：問題と既知baseline → 同値なcompletion自由度 → 有限候補の区間/遷移選択法 → controlled同値性と計算量 → 未使用構造・snapshot評価 → RPE等の一応用。

**中心図**：構造別cost地図、baseline対選択法のpaired比較、未使用条件の改善と失敗、古典前処理cost。RPE総cost図は補足でもよい。

**ここで閉じてよい**：同じ演算の合成改善と適用条件を示した時点。PRが決定論より安いこと、H12、状態準備、noiseを達成要件にしない。

**最大の採否条件**：既知の部分変換と相対basis融合を実装した後にも、共同選択による利益があること。ないなら無理に案Aに固執しない。

### 案B：固有値とスペクトル重みを用いたPFの使用条件

**核**：R4。PF選択法への拡張はR5を一部使ってもよいが、最初から新しい高次公式は不要。

**問いを一文で**：「エネルギーbiasの小さいPFが、位相推定で使える信号を保つための追加条件は何か。」

**完成時に言いたい主張**：「対象分枝のbiasとweight移送を分けた診断が、energy-only基準では判断できない使用可能δまたは不適格候補を予測した。」

**想定構成**：energy-only設計の限界 → weightとsignalの関係 → 安価な診断/条件付きbound → DF小系の直接参照 → 未使用条件とRPEの分枝・半径 → 限界。

**中心図**：biasとweightの二軸図、δに対する採否地図、推定weight対直接値、RPE信号・branch成功の検証、診断に要した古典cost。

**ここで閉じてよい**：限定問題族に使える新しい診断または設計則と、その有効性が示せた時点。全量子化学系の厳密保証を共通要件にしない。

**最大の採否条件**：既知の粗いboundやenergy-only基準では足りず、追加診断が実際の選択を改善すること。単にw_0を全対角化で求めるだけでは着地点に届かない。

### 案C：構造間エネルギー差に対するPF誤差の予測と制御

**核**：R7。R4は状態・分枝の安全確認として必要な範囲で使う。

**問いを一文で**：「化学構造を変えるとき、誤差相殺を壊さない分解・PFを選べば、目的のエネルギー差を低cost化できるか。」

**完成時に言いたい主張**：「符号付きPF誤差の構造応答を使い、相殺の成立・破れを予測した。差分目的の選択が、絶対エネルギーを別々に最適化する対照より適切だった。」

**想定構成**：絶対値と差分の目的の違い → signed leading errorと構造応答 → 連続表現/差分設計 → 未使用geometry pair → 資源への含意 → 交差・rank変化等の破綻例。

**中心図**：真値とPFのエネルギー曲線、絶対biasと差分bias、C(x)と状態/分解の変化、独立最適化対差分設計、未知pairでの予測と破れ。

**ここで閉じてよい**：一つの明示した差分タスクで予測・設計の効果が示せた時点。RTE、全RPE、実用サイズ反応計算、force全体まで求める必要はない。

**最大の採否条件**：単に近い二点だからbiasも近いという自明な観察を超え、適用範囲・例外・選択法を示せること。

### 案D：ランダムtailの時間負担を考慮したエネルギー用PF

**核**：R5。R3は公平な資源比較のために必要。

**問いを一文で**：「energy-orderの改善とtail絶対時間の増加をどう両立させるか。」

**完成時に言いたい主張**：「限定したPF familyで、固有値精度とRTE負担のPareto関係を特徴づけ、その情報を用いた係数/公式選択が既存選択を改善した。」

**想定構成**：既存energy-PFとPR → weighted absolute timeの設計指標 → 係数条件とfamily → 内側H_D近似を戻した検証 → state/energy/RTE/cost比較 → 有効・無効領域。

**中心図**：error係数対tail負担、同精度総cost、次数と精度による選択、内側誤差を戻す前後の差。

**ここで閉じてよい**：有限familyに対する構成または選択原理と適用例。全stage数・全次数の最適公式を求める必要はない。

**最大の採否条件**：既存の固有値用PFやnear-integrable設計に対し、randomization固有の指標が新しい選択または説明を生むこと。

### 四案の使い分け

| 重視する成果 | 合う案 | 成功に必要な決定的な一件 |
|---|---|---|
| 実行可能な回路手法を残す | A | 強い既知baselineを超える列全体設計 |
| PFと量子位相推定の関係を明らかにする | B | 安価なweight診断による採否の改善 |
| 化学的な目的量へ大きく転換する | C | 未知geometry差の相殺/破れの予測と制御 |
| 数式・係数設計を主題にする | D | random-tail負担を含む新しい非劣位family/選択則 |

この表は研究者の好みや能力を決めつけるものではない。現在の証拠だけでAを自動採用する必要はなく、B/C/Dを選ぶ場合にも基盤を使い回せる。

<a id="s14"></a>
## 14. 今から行う判別と、その後の分岐

### 14.1 まず、追加検証一覧ではなく研究仮説を固定する

各候補について「得たい主張」「最も近い先行研究」「差分」「最小着地点」を一枚にする。本書の各節がその原案になる。

特に、以下の言い換えができるかを確認する。

- A：「部分基底変換を実装した」ではなく「基底変換の自由度を列全体で選ぶ」。
- B：「固有値/固有ベクトル誤差を区別した」ではなく「安価な量から使用条件を予測する」。
- C：「相殺を観測した」ではなく「構造間相殺を保つ設計・破れを予測する」。
- D：「高次化した」ではなく「random-tail負担を含めてenergy-PFを設計する」。

ここが先行研究と区別できない場合、重い計算を増やしても新規性は解決しない。

### 14.2 最初の三つの判別候補

以下は全て必須ではない。方向を一つに決めない段階で比較するための、小さい独立したpilotである。具体的なsample数や実行時間は、入力と計算環境を確認してから決める。

#### 判別P-A：強い既知baselineの後にも列最適化の余地があるか

対象：R1/案A。

内容：単発の既知部分基底変換と隣接相対basis融合を対照へ入れる。同basis異support、異basis、長短runの小さい列を作り、区間/補完の共同選択に利益が残るか比較する。

残す量：同じoperatorの数値残差、実装count/depth、有限候補oracleに対する損失、構造ごとの改善/悪化、古典生成cost。

進む条件：独立した複数構造で、既知baselineとの差を機構で説明できる。停止条件：既知最適化だけで差が消え、joint選択が選択・costを改善しない。

**不要な作業**：H4のδ/r最適化、長RPE総cost、H12。

#### 判別P-B：energy-only選択とsignal-aware選択が食い違うか

対象：R4/案B。

内容：小系のgap・δ・PF候補を少数変え、固有位相biasと対象weightを独立に測る。全対角化は最初の参照にだけ使い、state-action/低状態の診断候補が採否を予測できるか比べる。

残す量：signed bias、target weight、q別signal、診断値、energy-onlyとsignal-awareの選択、false acceptanceと過剰棄却。

進む条件：同程度のbiasでも信号の使用可否が違い、追加診断でその差を予測できる。停止条件：選んだ範囲で効果が常に無視できるか、既知の安価なboundだけで十分。

**不要な作業**：RTE再設計、全compiler再測定。

#### 判別P-C：構造方向に予測可能なsigned errorが存在するか

対象：R7/案C。

内容：一つの小系のgeometry familyを用意し、固定PFの符号付きbiasを複数δで確認する。独立表現生成と連続追跡を比較し、少数点から未使用geometry pairの差分biasが予測できるか調べる。

残す量：H/DFの対応、gap、状態重なり、C(x)、差分bias、表現の不連続、係数を差分目的にした場合の候補選択。

進む条件：非自明な差分タスクで、相殺と破れを予測・改善できる見通しがある。停止条件：相殺が単なる微小geometry間隔の結果で、予測や設計へ進めない。

**不要な作業**：RPE全round、sampling最適化、物理noise。

### 14.3 解析を先に選ぶ場合のP-D

案Dを重視する場合は、上のpilotの一つに代えて、有限stage familyのBCH/energy条件とΓ_RのPareto監査を行う。既存公式の比較と簡単な反例で、randomization固有の設計条件が残るか確認する。

「新しいPFができてから資源を見る」ではなく、構成可能なfamilyとtail負担の両方を先に見る。H_D内部を厳密扱いしたtoy結果と実際のfragment内PFを混同しない。

### 14.4 A0と既存レビュー6節の位置づけ

A0は最新proxyと既存q=16,32の証拠関係を整える作業であり、実施してもよい。ただし、テーマ再設計を始めるための必須関門ではない。新しく研究対象をR7へ変えるのに、旧H4 proxyが全domainで完成する必要はない。

δ/r/prefixの境界拡張はR3またはR5を主に残す場合へ回す。blind policy transferはR1のstrong-baseline確認の後に有用。決定論四次baselineはPRの広い優位性を主張する場合に必要で、回路合成そのものの論文へ一律に課さない。

### 14.5 pilot後の決め方

| pilot結果 | 主題候補 | 次にすること | しないこと |
|---|---|---|---|
| 強いbaseline後もjoint合成が効く | 案A | 構造予測と未使用instance | PR勝敗確定を完了条件に追加しない |
| biasは小さいがweight問題が選択を変える | 案B | 安価な診断と使用条件 | エネルギー係数だけの精密化へ戻らない |
| 差分biasを予測・保護できる | 案C | 差分目的設計と外部geometry | RTEを無理に組み込まない |
| energy/Γ_Rに新しい非劣位family | 案D | 内側誤差を戻し資源比較 | formal orderだけで評価しない |
| 複数に根拠が残る | 主題一つ＋補助一つ | 主張が共通する範囲だけ結合 | 全テーマを同時開発しない |
| どれにも独立した差分がない | 再定義 | 原因を整理し、R3/R6/R8の問いを絞る | 「検証数を増やせば新規性が出る」と考えない |

最後の行は研究不能という意味ではない。現在の仮説が支持されなかったと分かっただけであり、別の対象量・制約・アルゴリズムclassへ問題を移せる。

<a id="s15"></a>
## 15. 完了条件・中止条件・成果の表現

### 15.1 全テーマに共通する完了条件

**問いが一つに定まる。** 「DF、RTE、RPE、PFを全て改善する」ではなく、何を選ぶ／予測する研究かが明確である。

**対照が適切である。** 素朴な実装だけでなく、主張に直接関係する既知の工夫を入れたbaselineを置く。ただし目的が異なる全アルゴリズムを無制限に比較する必要はない。

**予測と評価を分ける。** 学習・policy選択に使った条件を独立holdoutと呼ばない。化学系で調整した量と、未使用系でも計算できる量を区別する。

**少なくとも一つの有用な成果が残る。** 新しい構成法、既知法の鋭い適用条件、安価な予測法、具体的な失敗機構、または強いbaselineを含む意思決定を変える資源評価である。

**主張のscopeを閉じる。** gate set、制御方式、Hamiltonian近似、対象量、sampling、状態準備、compilerの範囲を明示する。除外した要素を全部実装しないと研究が閉じないわけではない。

### 15.2 否定的結果を成果にする条件

「今回の区間は重なる」「この一系で勝たなかった」「このコードで効果が消えた」だけでは、広い否定的結論にはならない。

一方、次は成果になり得る。

- 明示した回路classで、局所最適化が大域costを悪化させる機構を特定する。
- 同じ目標の公平な最適化後に、特定の精度・表現領域でPRの利益が消える理由を示す。
- energy-only基準が安全でない条件と、それを検出する指標を示す。
- エネルギー差の相殺が失敗する条件を予測し、過大な時間刻みを避ける。

「全てに勝つ必要がない」と「何が出ても論文になる」は異なる。主張を支える説明と独立した証拠が必要である。

### 15.3 過剰な完了条件を置かない

以下を全候補へ共通に要求しない。

- H12以上を必ず計算すること。
- 目標q_maxの巨大回路を直接transpileすること。
- 全compilerで順位が同じになること。
- state preparationやnoiseまで含むこと。
- 全PF order・全分割を大域最適化すること。
- 数学的保証を持たない経験的研究を全て無効にすること。

必要なのは、その研究の主張に足りる範囲である。例えばR1の同値合成研究に、PR全体がqubitizationより安いことは必要ない。逆に「広く最も低costなエネルギー推定法」を主張するなら、比較・scopeの要求は大きくなる。

### 15.4 不確かさと選択の扱い

共通の5%や25%を新しい全研究の合否基準にしない。数学的同値性の数値許容、MCの標準誤差、model discrepancy、研究上意味のある差は別である。

候補間差より不確かさが大きい場合は、単にsampleを増やす前に、その差が研究主張の中心か確認する。主題が回路の仕組みなら、アルゴリズム間の数％の勝敗を確定する必要がない場合がある。

<a id="s16"></a>
## 16. 既存コードと先の追加検証をどう扱うか

### 16.1 再利用できるもの

| 基盤 | 主に使える候補 |
|---|---|
| 分子Hamiltonian、DF、固定snapshot | 全候補。ただしR7ではgeometry間対応を追加 |
| PF・state-action・固有位相参照 | R3、R4、R5、R7 |
| RTE event、Gaussian basis、controlled builder | R1、R2、R3、R5、R6、R8 |
| 境界補正・対応付きcompile | R1、R2、R3 |
| RPE shot・配分・資源集計 | R3、R4、R5、R8。R1/R7では応用用でよい |
| 証拠台帳・fingerprint・holdoutの設計 | 全候補。新規性そのものではない |

大きく転換しても、全コードを捨てる必要はない。同時に、再利用できるという理由で不要なRTEやRPEを主題へ組み込まない。

### 16.2 研究を変えても履歴を変えない

旧artifact、旧scope、否定された候補、予備検証は残す。新テーマを採る場合は、新しい研究目的文書を作り、何を維持し、何を変更し、何を完了要件から外すかを明記する。

T4/T7等の現行正式判断は、その変更を合意するまでは正式判断として残る。本書の案A–DやR1–R8を、既に採択された研究方針として台帳へ書き換えない。

### 16.3 現時点での提案

**今はレビュー6節をまとめて実行せず、案A・B・Cの核となる仮説を比較する。解析・係数設計を重視する場合は案Dを同じ比較対象に置く。**

A0は証拠整理として別枠で実施可能。次の重い計算は、選んだ主題候補を支持または棄却できるものだけにする。

研究の着地点は「現在の検証リストが埋まった」ではなく、次のいずれかである。

> **同じ演算をより良く合成できる。**<br>
> **その近似を使ってよい条件をより良く判断できる。**<br>
> **目的の差分量をより良く求める設計ができる。**<br>
> **ランダム化の負担に適したPFを選択・構成できる。**

このうち一つを、既知研究との差分と独立した証拠を伴って示すことが、最初の明確な到達点になる。

### 16.4 2026-09-25 実施更新：P-D現実化gate後の停止点

案A/B/Cの停止後にP-Dを独立pilotとして評価し、exact two-block Pareto監査に続く現実化
Go/No-Goを完了した。負時間finite-RTEのdense operator/control位相、fragment内部`H_D`誤差、
fresh `L_D=5`選択差のD1--D3は、事前固定した条件を全て通過した。

従ってP-Dを正式主研究候補へ進める。ただし、これは本書が重視する「主張に必要な判別だけを
先に行う」という停止点であり、直ちにH12、長RPE、full総costへ進む判断ではない。次は計算を
停止し、次の順で研究方針を再設計する。

1. randomized tail負担を含むPF選択／設計という研究RQを一文で固定する。
2. eigenvalue-optimized PF、near-integrable PF、randomized/partially randomized simulation、
   negative-time PF、cost-aware PF optimizationとの差分を監査する。
3. 修士研究・論文としての最小着地点を決める。
4. その主張に必要な最小の一般化・反証検証だけを新たに事前登録する。

D1はdense small-matrix oracle、D2/D3ではtail occurrenceをexactに保っており、compiled control、
H4 sampled operator、end-to-end RPE、最終総cost、H12は未評価である。詳細な数値とartifactは
[`docs/research_direction_pd_realization.md`](docs/research_direction_pd_realization.md)を参照する。

<a id="refs"></a>
## 17. 出典と確認範囲

### リポジトリ資料

全てcommit `898da8487aca30a176ce5931d34a923c28c34ac9`に固定する。文書に記載されたlocal結果を参照しており、本書作成時に新規実験や全test suiteを再実行していない。

[R1]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/PROJECT_MAP.md
[R2]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/docs/research/prevalidation_catalog_evidence_map.md
[R3]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/docs/research_direction_full_opt2.md
[R4]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/docs/research_direction_sequence_policy.md
[R5]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/docs/research_direction_ablation.md
[R6]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/docs/research_direction_pf_sensitivity.md
[R7]: https://github.com/HIROMU1015/Partially-Randomized-Trotter/blob/898da8487aca30a176ce5931d34a923c28c34ac9/src/trotterlib/research_direction_full_opt2_extension_analysis.py

- [R1：PROJECT_MAP][R1]：研究・実装・証拠の案内。
- [R2：evidence map][R2]：実施scopeと最新status。
- [R3：M06-F][R3]：coherent opt2最終結果と限界。
- [R4：sequence policy][R4]：WP06-bのtraining、holdout、回路policy。
- [R5：ablation][R5]：β・α・schedule・cost modelの寄与。
- [R6：PF sensitivity][R6]：係数感度、固定予算での選択不変、符号規約。
- [R7：fresh32統合コード][R7]：最新proxyと旧holdoutの接続に関する注意。

### 一次文献

確認日は2026-09-25。2026年のpreprintを含み、査読済み論文と区別して読む。下記は関連性の高い一次資料の照合であり、世界中の全関連論文に同一アイデアがないことを証明する網羅調査ではない。

[W1]: https://arxiv.org/abs/2503.05647
[W2]: https://arxiv.org/html/1808.02625v2
[W3]: https://arxiv.org/abs/1711.04789
[W4]: https://arxiv.org/abs/2102.12655
[W5]: https://arxiv.org/html/2412.16811v1
[W6]: https://arxiv.org/abs/2312.13282
[W7]: https://arxiv.org/html/2606.30741v1
[W8]: https://arxiv.org/html/2603.13495v1
[W9]: https://arxiv.org/html/2605.00745v1
[W10]: https://arxiv.org/abs/2212.07957
[W11]: https://arxiv.org/html/2602.18913v1
[W12]: https://arxiv.org/abs/2212.05952
[W13]: https://quantum-journal.org/papers/q-2023-11-06-1165/
[W14]: https://arxiv.org/html/2606.23544v1

| ID | 文献 | この設計での役割 |
|---|---|---|
| [W1][W1] | Günther et al., *Phase estimation with partially randomized time evolution*, arXiv:2503.05647 / PRX Quantum 7, 020332 (2026) | PR・RPE資源・factorization・少数軌道回転は既知の基準 |
| [W2][W2] | Motta et al., *Low rank representations for quantum simulation of electronic structure*, arXiv:1808.02625 | 部分基底変換と隣接basis融合の基準 |
| [W3][W3] | Kivlichan et al., *Quantum Simulation of Electronic Structure with Linear Depth and Connectivity*, arXiv:1711.04789 | Gaussian/fermionic回路の既存合成基盤 |
| [W4][W4] | Yi and Crosson, *Spectral Analysis of Product Formulas for Quantum Simulation*, arXiv:2102.12655 | PFの固有値・固有ベクトル解析 |
| [W5][W5] | Hejazi et al., *Better product formulas for quantum phase estimation*, arXiv:2412.16811v1 | energy-specific PFと摂動的誤差解析 |
| [W6][W6] | *Estimating Trotter Approximation Errors to Optimize Hamiltonian Partitioning for Lower Eigenvalue Errors*, arXiv:2312.13282 | 誤差推定を分割選択へ使う既存研究 |
| [W7][W7] | Casares et al., *Theory and practice of Trotter product formulas for quantum chemistry*, arXiv:2606.30741v1 | SPRINT/GRADE、統合設計、peak位置とweightの区別 |
| [W8][W8] | Cugini, Atif and Subaşı, *Resource-Optimal Importance Sampling for Randomized Quantum Algorithms*, arXiv:2603.13495v1 | 任意回路costを含むsampling最適化 |
| [W9][W9] | Bay-Smidt et al., *Quantum simulation of nanographenes and Trotter error cancellation*, arXiv:2605.00745v1 | 固定geometry励起差の既報と、構造間差分の問い |
| [W10][W10] | Oumarou et al., *Accelerating Quantum Computations of Chemistry Through Regularized Compressed Double Factorization*, arXiv:2212.07957 | 正則化圧縮DFという既存表現最適化 |
| [W11][W11] | Kronenberger, Erakovic and Reiher, *Trotter Error and Orbital Transformations in Quantum Phase Estimation*, arXiv:2602.18913v1 | 軌道変換とTrotter誤差の既知検討 |
| [W12][W12] | Kiss, Grossi and Roggero, *Importance sampling for stochastic quantum simulations*, arXiv:2212.05952 | qDRIFTのcost-aware sampling |
| [W13][W13] | Ni, Li and Ying, *On low-depth algorithms for quantum phase estimation*, Quantum 7, 1165 (2023) | 低深さ位相推定と総資源の既知交換関係 |
| [W14][W14] | Dai, Hasselgren and Kiumi, *Structure-Aware Variance Reduction for Unbiased Randomized Hamiltonian Simulation*, arXiv:2606.23544v1 | 構造・分散低減も既存の競合領域であることの補足 |

W1について、本文テキストと表示PDF画像で版の同期が完全でない取得があったため、本書は版に依存する新しい表の数値や式番号を根拠にしていない。部分基底変換に関する主張はW2の本文でも照合している。W7–W9等の具体的な未検討範囲は指定したHTML版の記述に基づく。

### 引用と提案の境界

本書の「Research question」「着地点」「判別」「四案」は本レビューの提案であり、出典論文がその成果を達成したという意味ではない。Cauchy–Schwarz、三角不等式、摂動論、有限状態reward等の出発式は既存数学の利用である。これらの式を載せただけで新規性が生まれるとは扱わない。

**最終判断：今ある結果は研究を選び直す材料として十分に具体的である。ただし、独立テーマとしての新規性は、回路改善率の大きさではなく、既知法との差分と、他者が使える予測・設計・適用条件によって決まる。**
